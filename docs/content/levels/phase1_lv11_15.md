# Phase 1: 변수와 입출력 (Lv 11 - 15) 상세 세트

각 레벨은 20개의 연습 세트로 구성됩니다.

---

## 🟦 Lv 11: 나눗셈의 심화 (//, %)
**설명**: 몫만 구하거나 나머지만 구하는 연산자를 실제 상황에 응용해 봅니다.

| Set | 코드 (code) | 가이드 (guide_ko) |
| :--- | :--- | :--- |
| 1 | `print(10 // 3)\nprint(10 % 3)` | 기본 몫(`//`)과 나머지(`%`) 연산입니다. |
| 2 | `total = 3661\nminutes = total // 60\nseconds = total % 60\nprint(minutes, seconds)` | 초를 분과 초로 변환하는 기본 로직입니다. |
| 3 | `total = 125\nbox_size = 10\nprint("Full boxes:", total // box_size)` | 전체 개수를 상자 크기로 나누어 가득 찬 상자 수를 구합니다. |
| 4 | `total = 125\nbox_size = 10\nprint("Left items:", total % box_size)` | 상자에 담고 남은 나머지 아이템 수를 구합니다. |
| 5 | `id = 980715\nyear = id // 10000\nprint("Birth Year:", year)` | 앞의 두 자리 숫자(년도)만 추출해 봅니다. |
| 6 | `n = 42\nprint("Is Even?", n % 2 == 0)` | 2로 나눈 나머지가 0인지 확인하여 짝수 판별을 합니다. |
| 7 | `files = 103\npage_limit = 10\nprint("Last page count:", files % page_limit)` | 마지막 페이지에 표시될 파일 개수를 구합니다. |
| 8 | `money = 5400\nprint("500 Won count:", money // 500)` | 특정 동전의 개수를 몫으로 구합니다. |
| 9 | `step = 15\nprint("Group ID:", step // 10)` | 10단위로 그룹을 지어 ID를 부여할 때 씁니다. |
| 10 | `cycle = 5\nprint("Index:", 7 % cycle)` | 순환하는 인덱스(0, 1, 2, 3, 4)를 만들 수 있습니다. |
| 20 | `seconds = 10000\nh = seconds // 3600\nm = (seconds % 3600) // 60\ns = seconds % 60\nprint(f"{h}h {m}m {s}s")` | [복습] 초를 시, 분, 초로 정교하게 나누어 출력합니다. |
| * | *(세트 11-19 생략, 수치 계산 응용 연습)* | ... |

---

## 🟦 Lv 12: 거듭제곱과 수학 함수 (Math Basics)
**설명**: 제곱(`**`), 반올림(`round`), 절대값(`abs`) 등 숫자 처리 기본 함수를 익힙니다.

| Set | 코드 (code) | 가이드 (guide_ko) |
| :--- | :--- | :--- |
| 1 | `print(2 ** 10)` | 2의 10승(1024)을 계산합니다. |
| 2 | `pi = 3.14159\nprint(round(pi, 2))` | 소수점 두 번째 자리까지 반올림합니다. |
| 3 | `diff = -15\nprint(abs(diff))` | 음수를 양수로 바꾸는 절대값 함수입니다. |
| 4 | `num = 123.456\nprint(int(num))` | 소수점을 버리고 정수 부분만 남깁니다. |
| 5 | `side = 5\narea = side ** 2\nprint(f"Square: {area}")` | 정사각형의 넓이 공식 연습입니다. |
| 6 | `radius = 7\narea = 3.14 * (radius ** 2)\nprint(area)` | 원의 넓이 계산 시 거듭제곱을 활용합니다. |
| 7 | `val = 15.67\nprint(round(val))` | 괄호에 숫자만 넣으면 가장 가까운 정수로 반올림합니다. |
| 8 | `print(round(2.5))\nprint(round(3.5))` | 파이썬의 반올림은 '가까운 짝수'로 가는 특성이 있으니 주의하세요. |
| 9 | `a, b = 10, -30\nprint(max(a, b), min(a, b))` | 두 수 중 큰 쪽과 작은 쪽을 찾아주는 함수입니다. |
| 10 | `print(pow(3, 4))` | `**` 연산자 대신 `pow()` 함수를 쓸 수도 있습니다. |
| 20 | `x1, x2 = 10, 25\ndistance = abs(x1 - x2)\nprint(f"Distance: {distance}")` | [복습] 두 점 사이의 거리를 계산할 때 절대값을 사용합니다. |
| * | *(세트 11-19 생략, 수치 가공 연습)* | ... |

---

## 🟦 Lv 13: 복합 대입 연산자 (Augmented Assignment)
**설명**: 변수의 현재 값을 활용하여 짧고 명확하게 값을 업데이트하는 법을 익힙니다.

| Set | 코드 (code) | 가이드 (guide_ko) |
| :--- | :--- | :--- |
| 1 | `score = 10\nscore += 5\nprint(score)` | `score = score + 5`의 단축형인 `+=`입니다. |
| 2 | `hp = 100\nhp -= 20\nprint(hp)` | 데미지를 입었을 때 값을 차감하는 `-=`입니다. |
| 3 | `val = 2\nval *= 8\nprint(val)` | 기존 값에 곱하는 `*=`입니다. |
| 4 | `total = 100\ntotal /= 4\nprint(total)` | 기존 값을 나누는 `/=`입니다. (결과는 실수) |
| 5 | `num = 10\nnum //= 3\nprint(num)` | 몫만 남기고 업데이트하는 `//=`입니다. |
| 6 | `n = 10\nn %= 3\nprint(n)` | 나머지만 남기고 업데이트하는 `%=`입니다. |
| 7 | `p = 2\np **= 3\nprint(p)` | 거듭제곱으로 업데이트하는 `**=`입니다. |
| 8 | `txt = "A"\ntxt += "B"\nprint(txt)` | 문자열 변수에도 `+=`를 써서 글자를 이어붙일 수 있습니다. |
| 9 | `msg = "Ha"\nmsg *= 3\nprint(msg)` | 문자열에 `*=`를 쓰면 글자가 반복 저장됩니다. |
| 10 | `count = 0\ncount += 1\ncount += 1\nprint(count)` | 카운터를 올리는 가장 전형적인 표현입니다. |
| 20 | `bal = 1000\nbal *= 1.1 # Interest\nbal -= 50  # Fee\nprint(f"Final: {bal:.0f}")` | [복습] 이자와 수수료를 반영하여 잔액을 업데이트합니다. |
| * | *(세트 11-19 생략, 다양한 상황의 업데이트 연습)* | ... |

---

## 🟦 Lv 14: 데이터 타입 변환 (Casting)
**설명**: 문자열을 숫자로, 숫자를 문자열로 상황에 맞게 강제 변환합니다.

| Set | 코드 (code) | 가이드 (guide_ko) |
| :--- | :--- | :--- |
| 1 | `s = "100"\nn = int(s)\nprint(n + 50)` | 문자열 `"100"`을 계산 가능한 정수형으로 바꿉니다. |
| 2 | `n = 42\ns = str(n)\nprint("Age is " + s)` | 숫자를 텍스트 결합을 위해 문자열형으로 바꿉니다. |
| 3 | `f = float("3.14")\nprint(f * 2)` | 소수점이 포함된 문자열을 실수형으로 바꿉니다. |
| 4 | `n = int(9.9)\nprint(n)` | 실수를 정수로 바꾸면 소수점 이하는 버려집니다. |
| 5 | `val = str(1 + 2)\nprint(val * 3)` | 계산된 결과를 문자열로 바꿔서 반복 출력해 봅니다. |
| 6 | `print(type(10), type("10"))` | `type()` 함수로 현재 변수의 자료형을 확인합니다. |
| 7 | `s = "  50  "\nprint(int(s.strip()))` | 공백을 먼저 지우고 숫자로 변환하는 습관이 필요합니다. |
| 8 | `b = bool(1)\nprint(b)` | 숫자 1은 논리형으로 변환하면 `True`가 됩니다. |
| 9 | `b = bool(0)\nprint(b)` | 숫자 0은 논리형으로 변환하면 `False`가 됩니다. |
| 10 | `b = bool("")\nprint(b)` | 빈 문자열은 `False`, 글자가 하나라도 있으면 `True`입니다. |
| 20 | `user_in = "2026"\ncur_year = int(user_in)\nprint(f"In 10 years: {cur_year + 10}")` | [복습] 입력받은 년도를 숫자로 바꿔 미래 년도를 계산합니다. |
| * | *(세트 11-19 생략, 타입 확인 및 변환 조합 연습)* | ... |

---

## 🟦 Lv 15: [Challenge] 금융 계산기 (Financial)
**설명**: [챌린지] 복합 연산자와 타입 변환을 사용하여 세금이나 환율을 계산합니다.

| Set | 코드 (code) | 가이드 (guide_ko) |
| :--- | :--- | :--- |
| 1 | `price = 1000\ntax = price * 0.1\nprint(f"Total: {price + tax}")` | 기본 부가세 계산 로직입니다. |
| 2 | `usd = "50"\nrate = 1350.5\nkrw = float(usd) * rate\nprint(f"Won: {krw:,.0f}")` | USD 문자열을 받아 원화로 환전(콤마 포맷 포함)합니다. |
| 3 | `salary = 5000000\nsal_1yr = salary * 12\nprint(f"Yearly: {sal_1yr:,}")` | 월급으로 연봉을 계산해 봅니다. |
| 4 | `val = 1000000\nval *= 1.05\nval *= 1.05\nprint(f"After 2 years: {val:,.0f}")` | 연 복리 이자 5%를 두 번 적용해 봅니다. |
| 5 | `total = 45500\npeople = 4\nprint(f"Ducth Pay: {total // people}")` | 사람 수로 나누어 떨어지는 몫만큼 더치페이 금액을 정합니다. |
| 6 | `item_cnt = 32\nbox_limit = 10\nprint(f"Need Boxes: {(item_cnt // box_limit) + 1}")` | 상자당 담는 개수를 계산하여 필요한 최소 상자 수를 구합니다. |
| 7 | `bill = 89.95\ntip = bill * 0.15\nprint(f"With Tip: {round(bill + tip, 2)}")` | 팁을 포함한 음식값을 구하고 반올림 처리합니다. |
| 8 | `raw_income = "2500.5"\nexp = 1200\nnet = float(raw_income) - exp\nprint(f"Net Income: {net}")` | 수입(문자열)과 지출(숫자)을 계산합니다. |
| 9 | `p1 = 15000\np2 = p1 * 0.8 # 20% DC\nprint(f"Discounted: {p2}")` | 할인율을 적용한 최종 가격을 구합니다. |
| 20 | `target = 100\ncurrent = 42\npct = current / target\nprint(f"Progress: [{'#' * (current//10):<10}] {pct:.1%}")` | [종합] 현재 진행률을 계산해 그래픽 바와 퍼센트로 표현합니다. |
| * | *(세트 11-19 생략, 금융/수치 응용 시나리오)* | ... |
