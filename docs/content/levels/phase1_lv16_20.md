# Phase 1: 변수와 입출력 (Lv 16 - 20) 상세 세트

각 레벨은 20개의 연습 세트로 구성됩니다.

---

## 🟦 Lv 16: 비교 연산자와 불리언 (Comparison)
**설명**: 두 값을 비교하여 참(True) 또는 거짓(False) 결과를 도출하는 법을 익힙니다.

| Set | 코드 (code) | 가이드 (guide_ko) |
| :--- | :--- | :--- |
| 1 | `a, b = 10, 20\nprint(a == b)\nprint(a != b)` | '같다'(`==`)와 '다르다'(`!=`) 비교입니다. |
| 2 | `a, b = 10, 20\nprint(a > b)\nprint(a < b)` | '크다'와 '작다' 비교 연산입니다. |
| 3 | `a, b = 10, 10\nprint(a >= b)\nprint(a <= b)` | '크거나 같다'와 '작거나 같다' 비교입니다. |
| 4 | `msg = "Python"\nprint(msg == "python")` | 파이썬은 대소문자를 엄격히 구분하므로 `False`가 나옵니다. |
| 5 | `print(10 == 10.0)` | 정수 10과 실수 10.0은 논리적으로 같은 값으로 간주됩니다. |
| 6 | `age = 18\nprint(f"Is Adult? {age >= 19}")` | 나이 변수를 기준으로 성인 여부를 판단합니다. |
| 7 | `score = 85\nprint(f"Passed: {score > 80}")` | 성적이 기준 점수를 초과했는지 확인합니다. |
| 8 | `input_pw = "1234"\nsaved_pw = "1234"\nprint(input_pw == saved_pw)` | 비밀번호 일치 확인 로직의 기초입니다. |
| 9 | `v = "A"\nprint(v > "B")` | 문자도 알파벳 순서(ASCII 값)를 기준으로 크기 비교가 가능합니다. |
| 10 | `x = 5\nprint(0 < x < 10)` | 여러 비교를 한 줄로 이어 쓰는 복합 비교가 가능합니다. |
| 20 | `limit = 100\ncurrent = 105\nprint(f"Warning! [{current > limit}]")` | [복습] 수치가 한계치를 넘었을 때 경고 메시지를 불리언으로 표현합니다. |
| * | *(세트 11-19 생략, 수치 및 문자 비교 연습)* | ... |

---

## 🟦 Lv 17: 논리 연산자 (Logical: and, or, not)
**설명**: 여러 개의 조건을 결합하거나 반전시켜 복잡한 판단 기준을 만듭니다.

| Set | 코드 (code) | 가이드 (guide_ko) |
| :--- | :--- | :--- |
| 1 | `print(True and True)\nprint(True and False)` | `and`는 양쪽이 모두 참일 때만 `True`가 됩니다. |
| 2 | `print(True or False)\nprint(False or False)` | `or`는 한쪽만 참이어도 `True`가 됩니다. |
| 3 | `print(not True)\nprint(not False)` | `not`은 참을 거짓으로, 거짓을 참으로 뒤집습니다. |
| 4 | `age = 25\nhas_license = True\nprint(age >= 19 and has_license)` | 성인이면서 면허가 있는 가를 동시에 확인합니다. |
| 5 | `is_weekend = True\nis_holiday = False\nprint(is_weekend or is_holiday)` | 주말이거나 공휴일이면 쉴 수 있음을 나타내는 `or` 조건입니다. |
| 6 | `is_member = False\nprint(not is_member)` | 멤버가 아니면 혜택 제외 등 '부정(Negative)' 판단에 씁니다. |
| 7 | `score = 95\nprint(90 <= score <= 100)` | 점수가 90점대(90 이상 100 이하)인지 확인합니다. |
| 8 | `v = 10\nprint(v % 2 == 0 and v > 5)` | 짝수이면서 5보다 큰 조건입니다. |
| 9 | `user = "admin"\npw = "ok"\nprint(user == "admin" and pw == "ok")` | 아이디와 암호를 동시에 검수하는 기본 원리입니다. |
| 10 | `is_rainy = True\nhas_umbrella = False\nprint(is_rainy and not has_umbrella)` | 비는 오는데 우산은 없는 난처한 상황을 표현합니다. |
| 20 | `v1, v2, v3 = 10, 20, 30\nprint(v1 < v2 or v2 > v3 and v1 == 10)` | [복습] 연산자 우선순위(not > and > or)를 숙지하며 결과값을 판별합니다. |
| * | *(세트 11-19 생략, 논리 조합 연습)* | ... |

---

## 🟦 Lv 18: 멤버십과 포함 여부 (in)
**설명**: 문자열이나 목록 안에 특정 데이터가 들어있는지 확인하는 법을 익힙니다.

| Set | 코드 (code) | 가이드 (guide_ko) |
| :--- | :--- | :--- |
| 1 | `s = "Python"\nprint("P" in s)\nprint("p" in s)` | 대소문자를 구분하여 특정 글자가 포함되었는지 확인합니다. |
| 2 | `msg = "Hello World"\nprint(" " in msg)` | 문자열 내에 공백이 포함되어 있는지 확인합니다. |
| 3 | `ban_words = ["bad", "hate"]\nword = "bad"\nprint(word in ban_words)` | 금지어 목록(리스트)에 특정 단어가 있는지 확인합니다. |
| 4 | `ext = ".py"\nfile = "main.py"\nprint(file.endswith(ext))` | `in` 대신 전용 함수인 `endswith()`로 끝 문자를 확인합니다. |
| 5 | `target = "A"\nsource = "ABC"\nprint(target not in source)` | `not in`은 포함되어 있지 않을 때 `True`가 됩니다. |
| 6 | `log = "[ERROR] out of memory"\nprint("ERROR" in log)` | 로그 데이터에 에러 키워드가 있는지 검사합니다. |
| 7 | `data = "2026-01-07"\nprint("01" in data)` | 날짜 문자열에 특정 월(01월)이 포함되었는지 확인합니다. |
| 8 | `alphabet = "abcdefg"\nprint("hij" in alphabet)` | 불연속하거나 존재하지 않는 문자열 테스트입니다. |
| 9 | `code = "DEV-001"\nprint("-" in code)` | 코드 형식에 하이픈 구분자가 들어있는지 체크합니다. |
| 10 | `users = ["alice", "bob"]\nprint("charlie" not in users)` | 신규 사용자가 기존 목록에 없는지 확인할 때 씁니다. |
| 20 | `search = "PY"\nraw = "PYTHON3"\nprint(f"Found {search}: {search in raw}")` | [복습] 검색어 포함 여부를 f-string으로 결과를 보여줍니다. |
| * | *(세트 11-19 생략, 포함 여부 판단 응용)* | ... |

---

## 🟦 Lv 19: 문자 코드와 변환 (Characters)
**설명**: 문자와 숫자(ASCII/Unicode) 간의 관계를 이해하고 변환해 봅니다.

| Set | 코드 (code) | 가이드 (guide_ko) |
| :--- | :--- | :--- |
| 1 | `print(ord("A"))\nprint(ord("B"))` | `ord()`는 문자를 해당 숫자로 변환합니다 (A=65). |
| 2 | `print(chr(65))\nprint(chr(66))` | `chr()`은 숫자를 다시 문자로 변환합니다. |
| 3 | `code = ord("a")\nprint(chr(code + 1))` | 소문자 'a' 다음 문자인 'b'를 숫자로 계산해 출력합니다. |
| 4 | `print(ord("가"))\nprint(chr(44032))` | 한글과 같은 유니코드 문자도 처리가 가능합니다. |
| 5 | `s = " " \nprint(ord(s))` | 공백(Space) 문자의 코드값은 32입니다. |
| 6 | `v = ord("Z") - ord("A")\nprint(f"Alpha Range: {v}")` | 두 알파벳 사이의 거리를 계산해 봅니다. |
| 7 | `print(ord("1"), ord("2"))` | 숫자 모양 문자("1")의 실제 코드값은 49입니다. |
| 8 | `print(chr(128640))` | 우주선 이모지(🚀) 같은 특수 기호도 코드로 출력할 수 있습니다. |
| 9 | `val = 10\nprint(f"Code for {val}: {chr(val + 48)}")` | 0~9 숫자를 대응하는 숫자로 문자화하는 기법입니다. |
| 10 | `s = "PYTHON"\nprint(ord(s[0]))` | 문자열의 첫 글자 코드값만 추출해 봅니다. |
| 20 | `start = ord("A")\nend = ord("Z")\nprint(f"Range: {start} ~ {end}")` | [복습] 알파벳 대문자의 숫자 범위를 출력합니다. |
| * | *(세트 11-19 생략, 문자와 수치 매핑 연습)* | ... |

---

## 🟦 Lv 20: [Challenge] 기초 성적표 제작
**설명**: [챌린지] Phase 1을 마무리하며 복합적인 판단과 출력을 수행합니다.

| Set | 코드 (code) | 가이드 (guide_ko) |
| :--- | :--- | :--- |
| 1 | `math, eng = 85, 92\navg = (math + eng) / 2\nprint(f"AVG: {avg}")` | 두 과목의 평균을 구해 출력합니다. |
| 2 | `score = 88.5\nis_pass = score >= 85\nprint(f"Pass? {is_pass}")` | 합격 기준(85점)에 도달했는지 확인합니다. |
| 3 | `title = "FINAL REPORT"\nprint(f"{title:=^30}")` | 중앙 정렬 제목을 만듭니다. |
| 4 | `rank = 1\nprint(f"Rank: {'#' + str(rank)}")` | 랭킹 숫자를 문자화하여 장식과 결합합니다. |
| 5 | `is_perfect = (math == 100 or eng == 100)\nprint(f"Perfect? {is_perfect}")` | 한 과목이라도 100점인지 확인합니다 (or). |
| 6 | `is_stable = (80 <= math <= 100)\nprint(f"Stable? {is_stable}")` | 특정 과목 점수가 안정권인지 범위를 확인합니다. |
| 7 | `grade = "PASS"\nprint(f"[{grade:!^10}]")` | 등급을 강조 표시하여 출력합니다. |
| 8 | `math_up = math + 5 # Bonus\nprint(f"New Math: {math_up}")` | 기존 점수에 보너스를 더해 봅니다. |
| 9 | `msg = "Result: "\nmsg += "Approved"\nprint(msg)` | 문자열 변수에 상태를 덧붙여 최종 메시지를 만듭니다. |
| 10 | `s1, s2 = "80", "90"\nprint(f"Total: {int(s1) + int(s2)}")` | 문자열 형식의 점수를 숫자로 바꿔 합산합니다. |
| 20 | `h = "REPORT,2026,ADMIN"\nparts = h.split(",")\nprint(" - ".join(parts))` | [종합] 콤마로 구분된 헤더를 분해한 뒤 대시로 다시 합쳐 출력합니다. |
| * | *(세트 11-19 생략, 총괄적인 기초 문법 복습)* | ... |
