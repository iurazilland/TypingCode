# Phase 1: 변수와 입출력 (Lv 6 - 10) 상세 세트

각 레벨은 20개의 연습 세트로 구성됩니다.

---

## 🟦 Lv 6: 문자열 메서드 1 (splitting)
**설명**: 하나의 긴 문자열을 공백이나 특정 기호를 기준으로 쪼개는 법을 익힙니다.

| Set | 코드 (code) | 가이드 (guide_ko) |
| :--- | :--- | :--- |
| 1 | `data = "Apple Banana Cherry"\nitems = data.split()\nprint(items)` | `split()`은 공백을 기준으로 문자열을 리스트로 나눕니다. |
| 2 | `csv = "ID,Name,Email"\nfields = csv.split(",")\nprint(fields)` | 괄호 안에 구분자(`,`)를 넣으면 해당 문자를 기준으로 나눕니다. |
| 3 | `path = "/usr/local/bin"\nparts = path.split("/")\nprint(parts)` | 슬래시(`/`)를 기준으로 경로를 분해해 봅니다. |
| 4 | `date = "2026-01-07"\ny, m, d = date.split("-")\nprint(y, m, d)` | 나눈 결과를 각각의 변수에 한 번에 담을 수 있습니다. |
| 5 | `txt = "one:two:three"\nprint(txt.split(":"))` | 콜론(`:`) 구분자 연습입니다. |
| 6 | `s = "a b c d e"\nprint(s.split(maxsplit=2))` | `maxsplit`을 쓰면 앞에서부터 지정한 횟수만큼만 쪼갭니다. |
| 7 | `data = "root:x:0:0:root"\nuser_info = data.split(":")\nprint(user_info[0])` | 나눈 결과 중 첫 번째 요소(인덱스 0)만 추출합니다. |
| 8 | `colors = "red|blue|green"\nprint(colors.split("|"))` | 파이프(`|`) 기호 구분자 연습입니다. |
| 9 | `tag = "python#coding#dev"\nprint(tag.split("#"))` | 해시태그(`#`) 기호 구분자 연습입니다. |
| 10 | `msg = "Hello World From Python"\nwords = msg.split()\nprint(f"Total: {len(words)}")` | 쪼갠 단어의 총 개수를 출력해 봅니다. |
| 11 | `url = "www.google.com"\ndomain = url.split(".")\nprint(domain)` | 점(`.`)을 기준으로 도메인 주소를 분해합니다. |
| 12 | `logs = "ER-01 ER-02 ER-03"\nlist_log = logs.split()\nprint(list_log)` | 공백이 여러 개인 에러 로그 분해 예시입니다. |
| 13 | `name = "Guido van Rossum"\nn = name.split()\nprint(n[-1])` | 성과 이름을 나누고 마지막 요소(성)만 출력합니다. |
| 14 | `vals = "10 20 30"\nprint(vals.split())` | 문자열 형태의 숫자들을 쪼개 봅니다. |
| 15 | `text = "apple, orange, grape"\nprint(text.split(", "))` | 콤마와 공백(`, `)을 통째로 구분자로 씁니다. |
| 16 | `line = "tab\\tspace\\nnewline"\nprint(line.split())` | `split()`은 탭이나 줄바꿈도 공백으로 간주해 쪼갭니다. |
| 17 | `code = "PY.2026.01"\nparts = code.split(".")\nprint(parts[1])` | 점으로 나눈 것 중 연도(두 번째 요소)만 가져옵니다. |
| 18 | `raw = ":::admin:::"\nprint(raw.split(":"))` | 연속된 구분자가 있을 때의 결과(빈 문자열)를 확인합니다. |
| 19 | `info = "KR/Seoul/IT"\nprint(info.split("/", 1))` | 첫 번째 슬래시에서만 한 번 쪼갭니다. |
| 20 | `user = "id:pw:name:age"\nfields = user.split(":")\nprint(f"User {fields[2]} is {fields[3]} years old.")` | [복습] 나눈 데이터를 f-string과 결합해 문장을 만듭니다. |

---

## 🟦 Lv 7: 문자열 메서드 2 (join & strip)
**설명**: 쪼개진 글자들을 하나로 합치거나 불필요한 공백을 제거하는 법을 익힙니다.

| Set | 코드 (code) | 가이드 (guide_ko) |
| :--- | :--- | :--- |
| 1 | `words = ["Python", "is", "cool"]\nmsg = " ".join(words)\nprint(msg)` | 리스트 요소들을 공백(` `)으로 연결해 하나의 문자열을 만듭니다. |
| 2 | `tags = ["dev", "python", "code"]\nresult = "#".join(tags)\nprint("#" + result)` | 해시태그 리스트를 `#`으로 연결하여 출력합니다. |
| 3 | `path = ["usr", "local", "bin"]\nprint("/".join(path))` | 폴더 이름을 슬래시로 합쳐 경로를 만듭니다. |
| 4 | `txt = "   hello   "\nprint(txt.strip())` | `strip()`은 문자열 양쪽의 공백을 말끔히 제거합니다. |
| 5 | `txt = "---Title---"\nprint(txt.strip("-"))` | 괄호 안에 특정 문자를 넣으면 그 문자만 제거합니다. |
| 6 | `chars = ["A", "B", "C"]\nprint("".join(chars))` | 빈 문자열(`""`)로 `join`하면 모든 글자가 딱 붙어서 합쳐집니다. |
| 7 | `data = "   data\\n"\nprint(data.strip())` | `strip()`은 줄바꿈 문자(`\\n`)도 함께 지워줍니다. |
| 8 | `date = ["2026", "01", "07"]\nprint("-".join(date))` | 날짜 리스트를 `-` 기호로 묶습니다. |
| 9 | `msg = "///system///"\nprint(msg.lstrip("/"))` | `lstrip()`은 왼쪽(Left)에 있는 문자만 지웁니다. |
| 10 | `msg = "///system///"\nprint(msg.rstrip("/"))` | `rstrip()`은 오른쪽(Right)에 있는 문자만 지웁니다. |
| 11 | `nums = ["1", "2", "3"]\nprint("|".join(nums))` | 숫자 문자열 리스트를 파이프로 연결합니다. |
| 12 | `raw = "  [INFO]  "\nprint(raw.strip().strip("[]"))` | 공백을 지운 뒤 대괄호까지 연속해서 지웁니다. |
| 13 | `lines = ["Line 1", "Line 2", "Line 3"]\nprint("\\n".join(lines))` | 줄바꿈 문자(`\\n`)로 `join`하면 여러 줄 텍스트가 됩니다. |
| 14 | `raw_id = "   user_123   "\nprint(raw_id.strip().upper())` | 공백을 제거하고 바로 대문자로 바꿉니다. |
| 15 | `lst = ["Admin", "PowerUser"]\nprint(", ".join(lst))` | 콤마와 공백으로 사용자 등급을 나열합니다. |
| 16 | `txt = "!!!Danger!!!"\nprint(txt.strip("!"))` | 양 끝의 느낌표만 골라 지워봅니다. |
| 17 | `color = "  BLUE  "\nprint(f"Color: [{color.strip()}]")` | f-string 안에서 `strip` 결과를 바로 보여줍니다. |
| 18 | `seq = ["T", "G", "C"]\nprint("-".join(seq))` | DNA 염기쌍처럼 문자를 하이픈으로 잇습니다. |
| 19 | `raw = "\\tcontent\\n"\nprint(raw.strip())` | 탭과 줄바꿈이 섞인 문자열을 청소합니다. |
| 20 | `user_list = ["alice", "bob", "charlie"]\nfinal = ", ".join(user_list)\nprint(f"Online: {final.upper()}")` | [복습] 유저 목록을 합친 뒤 전체 대문자로 출력합니다. |

---

## 🟦 Lv 8: 멀티라인 문자열 (Triple Quotes)
**설명**: 여러 줄의 텍스트를 변수에 담고 그대로 출력하는 법을 익힙니다.

| Set | 코드 (code) | 가이드 (guide_ko) |
| :--- | :--- | :--- |
| 1 | `doc = """Line 1\\nLine 2\\nLine 3"""\nprint(doc)` | 따옴표 세 개(`"""`)를 쓰면 여러 줄 입력을 시작합니다. |
| 2 | `intro = """\\nHello, Python.\\nHow are you?\\n"""\nprint(intro)` | 양 끝에 빈 줄이 포함된 멀티라인 텍스트입니다. |
| 3 | `art = """\\n  *\\n ***\\n*****\\n"""\nprint(art)` | 간단한 별 모양 아트(ASCII Art)를 변수에 담습니다. |
| 4 | `sql = """\\nSELECT *\\nFROM users\\nWHERE id = 1\\n"""\nprint(sql)` | SQL 쿼리 문을 정의할 때 매우 유용합니다. |
| 5 | `log = """[ERR] System Crash\\n[ERR] Memory Leak\\n"""\nprint(log)` | 여러 줄의 에러 로그를 한 번에 저장합니다. |
| 6 | `story = """Once upon\\na time..."""\nprint(story)` | 이야기의 단락을 나눌 때 사용합니다. |
| 7 | `menu = """\\n1. Start\\n2. Option\\n3. Exit\\n"""\nprint(menu)` | 프로그램의 메뉴를 정의하는 예시입니다. |
| 8 | `txt = """Hello "Alice"\\nIt's yours."""\nprint(txt)` | 삼중 따옴표 안에서는 따옴표를 이스케이프 없이 쓸 수 있습니다. |
| 9 | `box = """\\n+-----+\\n| LOG |\\n+-----+\\n"""\nprint(box)` | 텍스트로 만든 박스 모양 예제입니다. |
| 10 | `code = """\\ndef hello():\\n    print("Hi")\\n"""\nprint(code)` | 파이썬 코드 자체를 문자열로 담아 봅니다. |
| 11 | `header = """\\n--- HEADER ---\\nAUTHOR: DEV\\n--------------\\n"""\nprint(header)` | 리포트의 헤더 정보를 저장합니다. |
| 20 | `banner = """\\n****************\\n* WELCOME TO   *\\n* PYTHON TYPING *\\n****************\\n"""\nprint(banner.strip())` | [복습] 삼중 따옴표 배너를 만들고 양 끝 공백을 지워 출력합니다. |
| * | *(세트 12-19 생략, 위와 유사한 형태)* | ... |

---

## 🟦 Lv 9: 문자열 슬라이싱 (Slicing)
**설명**: 문자열의 인덱스를 번호로 지정하여 원하는 부분만 잘라냅니다.

| Set | 코드 (code) | 가이드 (guide_ko) |
| :--- | :--- | :--- |
| 1 | `s = "Hello"\nprint(s[0], s[1])` | `[0]`은 첫 번째 글자, `[1]`은 두 번째 글자를 의미합니다. |
| 2 | `s = "Python"\nprint(s[0:2])` | `[0:2]`는 인덱스 0번부터 2번 '전'까지(0, 1) 자릅니다. |
| 3 | `s = "Python"\nprint(s[2:])` | `[2:]`는 2번 인덱스부터 끝까지 모두 가져옵니다. |
| 4 | `s = "Python"\nprint(s[:2])` | `[:2]`는 처음부터 2번 전까지 가져옵니다. |
| 5 | `s = "Python"\nprint(s[-1])` | `[-1]`은 문자열의 가장 마지막 글자를 가져옵니다. |
| 6 | `s = "12345"\nprint(s[-2:])` | `[-2:]`는 뒤에서 두 번째 글자부터 끝까지 가져옵니다. |
| 7 | `s = "A-B-C-D"\nprint(s[::2])` | `[::2]`는 2칸씩 띄어가며 글자를 가져옵니다(A, B, C, D). |
| 8 | `s = "Hello"\nprint(s[::-1])` | `[::-1]`은 문자열을 거꾸로 뒤집습니다. |
| 9 | `id = "20260107"\nprint(id[:4], id[4:6], id[6:])` | 연, 월, 일을 슬라이싱으로 각각 분리합니다. |
| 10 | `txt = "Programming"\nprint(f"Start: {txt[:3]}")` | 앞 글자 3개만 추출해 f-string으로 보여줍니다. |
| 20 | `p = "PY-12345-LOG"\ncode, mid, tail = p.split("-")\nprint(mid[2:])` | [복습] split으로 나눈 것 중 중간 번호의 뒷부분만 슬라이싱합니다. |
| * | *(세트 11-19 생략, 슬라이싱 조합 연습)* | ... |

---

## 🟦 Lv 10: [Challenge] 로그 데이터 파서
**설명**: [챌린지] 지금까지 배운 split, join, strip, slicing을 복합적으로 사용합니다.

| Set | 코드 (code) | 가이드 (guide_ko) |
| :--- | :--- | :--- |
| 1 | `log = "TIMESTAMP:2026-01-07:INFO:INIT"\nparts = log.split(":")\nprint(parts[1])` | 콜론으로 나눈 뒤 날짜 부분만 가져옵니다. |
| 2 | `raw = "  CMD:/run/app  "\ncmd = raw.strip().split(":")[1]\nprint(cmd)` | 공백을 지우고 명령 경로 부분만 변수에 담습니다. |
| 3 | `info = "KR,Seoul,20C"\nitems = info.split(",")\nprint(" | ".join(items))` | 콤마 구분을 파이프 구분으로 변환합니다. |
| 4 | `user = "ID_8801:Active"\nid_part = user.split(":")[0]\nprint(id_part[3:])` | ID에서 번호 부분(`8801`)만 잘라냅니다. |
| 5 | `msg = "  [SYSTEM UPDATING]  "\nprint(msg.strip(" []"))` | 공백과 대괄호를 한꺼번에 지워 알맹이만 남깁니다. |
| 6 | `path = "/home/user/code/main.py"\nfilename = path.split("/")[-1]\nprint(filename)` | 경로에서 마지막 파일 이름만 뽑아냅니다. |
| 7 | `data = "010-1234-5678"\nparts = data.split("-")\nprint("****".join([parts[0], parts[2]]))` | 중간 번호를 별표로 가리는 마스킹 예제입니다. |
| 8 | `name = "GUIdo VAN rossum"\nwords = name.split()\nprint(words[0].capitalize(), words[2].capitalize())` | 이름을 쪼개서 각 단어의 첫 글자만 대문자로 만듭니다. |
| 9 | `ver = "v2.5.12"\nprint(f"Major: {ver[1:2]}, Minor: {ver[3:]}")` | 버전 문자열에서 주 버전과 부 버전을 분리합니다. |
| 10 | `raw = "    Python  "\nclean = raw.strip()\nprint(clean[0] + "*" * (len(clean)-1))` | 첫 글자 빼고 나머지를 별표 처리하는 기초 로직입니다. |
| 20 | `logLine = "2026-01-07 | ERROR | Low Memory!"\nparts = logLine.split(" | ")\nprint(f"[{parts[1]}] At {parts[0]}: {parts[2].upper()}")` | [종합] 로그 라인을 분해하여 다시 포맷팅된 문장으로 재구성합니다. |
| * | *(세트 11-19 생략, 조합 및 응용 연습)* | ... |
