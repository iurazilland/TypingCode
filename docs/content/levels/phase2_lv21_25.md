# Phase 2: 리스트와 복합 자료구조 (Lv 21 - 25) 상세 세트

각 레벨은 20개의 연습 세트로 구성됩니다.

---

## 🟦 Lv 21: 리스트 생성과 인덱싱 (List Basics)
**설명**: 여러 개의 데이터를 하나의 주머니(List)에 담고 번호로 꺼내는 법을 익힙니다.

| Set | 코드 (code) | 가이드 (guide_ko) |
| :--- | :--- | :--- |
| 1 | `nums = [1, 2, 3]\nprint(nums)` | 대괄호`[]`를 사용하여 숫자가 담긴 리스트를 만듭니다. |
| 2 | `colors = ["red", "blue"]\nprint(colors[0])` | 리스트의 첫 번째 항목(인덱스 0)을 가져옵니다. |
| 3 | `mix = ["A", 10, True]\nprint(mix)` | 파이썬 리스트는 서로 다른 타입의 데이터를 함께 담을 수 있습니다. |
| 4 | `nums = [10, 20, 30]\nnums[1] = 99\nprint(nums)` | 리스트의 특정 위치 값을 다른 값으로 변경할 수 있습니다. |
| 5 | `items = ["Phone", "Pad"]\nprint(len(items))` | `len()` 함수로 리스트에 들어있는 항목 개수를 확인합니다. |
| 6 | `nums = [1, 2, 3]\nprint(nums[-1])` | `-1` 인덱스는 가장 마지막 항목을 뜻합니다. |
| 7 | `empty = []\nprint(empty)` | 아무것도 들어있지 않은 빈 리스트를 만들 수 있습니다. |
| 8 | `alphabet = ["a", "b", "c", "d"]\nprint(alphabet[1:3])` | 슬라이싱`[:]`을 통해 리스트의 일부만 잘라냅니다. |
| 9 | `data = [1, 2, 3]\nprint(1 in data)` | `in` 연산자로 리스트 안에 특정 값이 있는지 확인합니다. |
| 10 | `v = [[1, 2], [3, 4]]\nprint(v[0][1])` | 리스트 안에 리스트가 들어있는 중첩 리스트 예제입니다. |
| 20 | `p = ["Admin", "Gold", 1250]\nprint(f"User {p[0]} is {p[1]} rank.")` | [복습] 유저 정보를 리스트에 담고 f-string으로 출력합니다. |
| * | *(세트 11-19 생략, 리스트 구조 연습)* | ... |

---

## 🟦 Lv 22: 데이터 추가와 결합 (Append & Extend)
**설명**: 리스트에 새로운 데이터를 뒤에 붙이거나 다른 리스트와 합치는 법을 익힙니다.

| Set | 코드 (code) | 가이드 (guide_ko) |
| :--- | :--- | :--- |
| 1 | `fruits = ["apple"]\nfruits.append("banana")\nprint(fruits)` | `append()`는 데이터를 리스트의 가장 뒤에 추가합니다. |
| 2 | `nums = [1, 2]\nnums.append(3)\nprint(nums)` | 숫자를 리스트 뒤에 덧붙입니다. |
| 3 | `users = []\nusers.append("alice")\nprint(users)` | 빈 리스트에 첫 번째 요소를 추가하는 과정입니다. |
| 4 | `a = [1, 2]\nb = [3, 4]\nprint(a + b)` | `+` 연산자를 사용하여 두 리스트를 하나로 합칠 수 있습니다. |
| 5 | `lst = ["A"]\nlst *= 3\nprint(lst)` | `*` 연산자로 리스트의 요소들을 반복해서 늘립니다. |
| 6 | `nums = [1, 2]\nnums.extend([3, 4])\nprint(nums)` | `extend()`는 다른 리스트의 모든 요소를 가져와 합칩니다. |
| 7 | `cart = ["Milk"]\ncart.append("Bread")\nprint(f"Items: {len(cart)}")` | 항목을 추가한 뒤 전체 개수를 확인해 봅니다. |
| 8 | `q1 = ["Jan", "Feb"]\nq2 = ["Mar", "Apr"]\nyear = q1 + q2\nprint(year)` | 분기별 리스트를 하나로 통합합니다. |
| 9 | `colors = ["Red"]\ncolors.append("Blue")\nprint(colors[1])` | 추가된 항목이 인덱스 1번에 위치함을 확인합니다. |
| 10 | `msg = ["Hi"]\nmsg.append("!")\nprint("".join(msg))` | 리스트에 요소를 추가한 뒤 `join`으로 합쳐 봅니다. |
| 20 | `tags = ["python"]\ntags.append("dev")\nprint(" #".join([""] + tags).strip())` | [복습] 태그를 추가한 후 해시태그 형식으로 출력합니다. |
| * | *(세트 11-19 생략, 데이터 축적 연습)* | ... |

---

## 🟦 Lv 23: 특정 위치 삽입과 삭제 (Insert & Delete)
**설명**: 리스트의 원하는 위치에 값을 넣거나, 값을 찾아 지우는 법을 익힙니다.

| Set | 코드 (code) | 가이드 (guide_ko) |
| :--- | :--- | :--- |
| 1 | `nums = [1, 3]\nnums.insert(1, 2)\nprint(nums)` | `insert(index, value)`는 지정한 위치에 값을 끼워넣습니다. |
| 2 | `colors = ["Red", "Blue"]\ncolors.insert(0, "Black")\nprint(colors)` | 맨 앞(인덱스 0)에 새로운 색상을 삽입합니다. |
| 3 | `nums = [1, 2, 3]\nnums.remove(2)\nprint(nums)` | `remove(value)`는 리스트에서 해당 값을 찾아 지웁니다. |
| 4 | `data = [10, 20, 30]\ndel data[1]\nprint(data)` | `del` 키워드를 사용하여 특정 인덱스의 값을 지웁니다. |
| 5 | `users = ["A", "B", "C"]\nlast = users.pop()\nprint(users, last)` | `pop()`은 마지막 요소를 제거함과 동시에 그 값을 반환합니다. |
| 6 | `users = ["A", "B", "C"]\nfirst = users.pop(0)\nprint(users, first)` | 인덱스를 지정하여 `pop(0)`을 하면 첫 번째 요소를 지우고 가져옵니다. |
| 7 | `lst = [1, 1, 2]\nlst.remove(1)\nprint(lst)` | `remove`는 중복된 값이 있으면 가장 앞에 있는 것 하나만 지웁니다. |
| 8 | `nums = [1, 2, 3]\nnums.clear()\nprint(nums)` | `clear()`는 리스트의 모든 요소를 지워 빈 리스트로 만듭니다. |
| 9 | `items = ["P1", "P2"]\nitems.insert(len(items), "P3")\nprint(items)` | `len`을 이용해 가장 마지막에 삽입하는 `append`와 같은 동작입니다. |
| 10 | `queue = ["User1", "User2"]\nqueue.pop(0)\nprint("Next:", queue[0])` | 대기열에서 첫 손님을 지우고 다음 손님을 확인합니다. |
| 20 | `undo_stack = ["Page1", "Page2"]\nundo_stack.append("Page3")\nundo_stack.pop()\nprint(undo_stack)` | [복습] 스택(Stack) 구조처럼 데이터를 쌓았다가 마지막 것을 지워봅니다. |
| * | *(세트 11-19 생략, 삽입/삭제 변형 연습)* | ... |

---

## 🟦 Lv 24: 리스트 슬라이싱 심화 (Slicing Plus)
**설명**: 여러 단계로 뛰어넘거나 거꾸로 뒤집는 등 슬라이싱의 고급 기법을 익힙니다.

| Set | 코드 (code) | 가이드 (guide_ko) |
| :--- | :--- | :--- |
| 1 | `n = [0, 1, 2, 3, 4, 5]\nprint(n[0:4:2])` | `[시작:끝:간격]` 형식으로 0번부터 4번 전까지 2칸씩 가져옵니다. |
| 2 | `n = [0, 1, 2, 3, 4, 5]\nprint(n[::2])` | 전체 리스트에서 짝수 인덱스(0, 2, 4)만 가져옵니다. |
| 3 | `n = [0, 1, 2, 3, 4, 5]\nprint(n[1::2])` | 전체 리스트에서 홀수 인덱스(1, 3, 5)만 가져옵니다. |
| 4 | `n = [1, 2, 3]\nprint(n[::-1])` | 리스트를 역순으로 뒤집은 새 리스트를 만듭니다. |
| 5 | `n = [1, 2, 3, 4, 5]\nprint(n[3:0:-1])` | 뒤에서부터 거꾸로 슬라이싱하는 예제입니다 (3, 2, 1). |
| 6 | `msg = list("PYTHON")\nprint(msg[:3])` | 문자열을 리스트로 바꾼 뒤 앞부분만 잘라냅니다. |
| 7 | `nums = [1, 2, 3]\nnums[1:3] = [8, 9]\nprint(nums)` | 슬라이싱 범위를 통째로 다른 리스트로 교체할 수 있습니다. |
| 8 | `nums = [1, 2, 3, 4]\nnums[1:3] = []\nprint(nums)` | 슬라이싱 범위에 빈 리스트를 대입하면 해당 구간이 삭제됩니다. |
| 9 | `a = [1, 2, 3]\nb = a[:]\nprint(b == a, b is a)` | `[:]`는 리스트의 전체 복사본을 만듭니다 (값은 같으나 객체는 다름). |
| 10 | `data = [10, 20, 30, 40, 50]\nprint(data[-3:])` | 뒤에서부터 세 번째 항목부터 끝까지 가져옵니다. |
| 20 | `s = "Racecar"\nprint(s.lower() == s.lower()[::-1])` | [복습] 문자열을 뒤집어 원래와 같은지 확인(회문, Palindrome)해 봅니다. |
| * | *(세트 11-19 생략, 슬라이싱 응용 연습)* | ... |

---

## 🟦 Lv 25: [Challenge] 장바구니 관리 시스템
**설명**: [챌린지] 리스트의 추가, 삭제, 삽입, 슬라이싱을 종합적으로 사용합니다.

| Set | 코드 (code) | 가이드 (guide_ko) |
| :--- | :--- | :--- |
| 1 | `cart = ["Apple", "Milk"]\ncart.append("Bread")\nprint(cart)` | 물품 하나를 장바구니 뒤에 추가합니다. |
| 2 | `cart.insert(1, "Egg")\nprint(f"Top: {cart[0]}, Next: {cart[1]}")` | 특정 위치에 중요 물품을 끼워넣습니다. |
| 3 | `cart.remove("Milk")\nprint(f"Items: {len(cart)}")` | 구매를 취소한 물품을 이름으로 찾아 삭제합니다. |
| 4 | `bought = cart.pop(0)\nprint(f"Paid: {bought}, Left: {len(cart)}")` | 첫 번째 물건을 결제하고 목록에서 지웁니다. |
| 5 | `more = ["Water", "Salt"]\ncart += more\nprint(cart)` | 추가 리스트를 장바구니에 합칩니다. |
| 6 | `print(f"Latest 2: {cart[-2:]}")` | 마지막으로 담은 물건 2개를 확인합니다. |
| 7 | `cart.sort()\nprint(f"Sorted: {cart}")` | 장바구니를 가나다 순으로 정렬해 봅니다. |
| 8 | `idx = cart.index("Water")\nprint(f"Water is at #{idx}")` | 특정 물건이 몇 번째에 있는지 위치를 찾습니다. |
| 9 | `cart[0] = "Premium " + cart[0]\nprint(cart[0])` | 첫 번째 물품의 이름을 수정해 봅니다. |
| 10 | `print(f"Cart empty? {len(cart) == 0}")` | 장바구니가 비었는지 확인하는 로직입니다. |
| 20 | `wishlist = ["PC", "Mic"]\ncart.extend(wishlist)\nprint(f"FINAL CART ({len(cart)}): {', '.join(cart)}")` | [종합] 위시리스트를 합친 뒤 예쁘게 나열하여 최종 목록을 보여줍니다. |
| * | *(세트 11-19 생략, 장바구니 관리 시나리오 연습)* | ... |
