# Phase 2: 리스트와 복합 자료구조 (Lv 26 - 30) 상세 세트

각 레벨은 20개의 연습 세트로 구성됩니다.

---

## 🟦 Lv 26: 탐색과 카운팅 (Searching & Count)
**설명**: 리스트 안에서 특정 데이터의 개수를 세거나 위치를 찾는 법을 익힙니다.

| Set | 코드 (code) | 가이드 (guide_ko) |
| :--- | :--- | :--- |
| 1 | `nums = [1, 2, 2, 3, 2]\nprint(nums.count(2))` | `count(value)`는 리스트에 해당 값이 몇 개 있는지 알려줍니다. |
| 2 | `colors = ["red", "blue", "red"]\nprint(colors.count("red"))` | 문자열 데이터의 출현 횟수를 세어 봅니다. |
| 3 | `items = ["A", "B", "C"]\nprint(items.index("B"))` | `index(value)`는 해당 값이 처음 나타나는 위치(인덱스)를 반환합니다. |
| 4 | `data = [10, 20, 30, 20]\nprint(data.index(20))` | 중복된 값이 있으면 가장 앞에 있는 인덱스를 알려줍니다. |
| 5 | `nums = [1, 5, 7]\nprint(10 in nums)` | `in` 연산자로 존재 여부만 True/False로 신속히 확인합니다. |
| 6 | `s = "banana"\nmsg = list(s)\nprint(msg.count("a"))` | 문자열을 리스트로 바꾼 뒤 특정 알파벳의 개수를 셉니다. |
| 7 | `scores = [90, 80, 70]\nprint(scores.index(max(scores)))` | 최고점 점수가 몇 번째 인덱스에 있는지 찾습니다. |
| 8 | `val = 100\nlst = [10, 20, 30]\nprint(lst.count(val) > 0)` | `count` 결과가 0보다 큰지 확인하여 포함 여부를 판단합니다. |
| 9 | `users = ["alice", "bob", "alice"]\nprint(f"Alice count: {users.count('alice')}")` | 특정 유저가 목록에 몇 번 등장하는지 f-string으로 출력합니다. |
| 10 | `data = [1, 2, 3, 4, 5]\nidx = data.index(3)\nprint(data[idx:])` | 값을 찾아낸 인덱스부터 끝까지 잘라내어 출력해 봅니다. |
| 20 | `p = ["Buy", "Sell", "Buy", "Buy"]\nprint(f"Total Buy Actions: {p.count('Buy')}")` | [복습] 거래 기록 리스트에서 'Buy' 액션의 총 횟수를 출력합니다. |
| * | *(세트 11-19 생략, 리스트 탐색 응용 연습)* | ... |

---

## 🟦 Lv 27: 정렬과 뒤집기 (Sort & Reverse)
**설명**: 리스트의 순서를 가나다 순이나 크기 순으로 정렬하는 법을 익힙니다.

| Set | 코드 (code) | 가이드 (guide_ko) |
| :--- | :--- | :--- |
| 1 | `nums = [3, 1, 4, 2]\nnums.sort()\nprint(nums)` | `sort()`는 리스트를 오름차순(작은 것부터)으로 직접 정렬합니다. |
| 2 | `nums = [3, 1, 4, 2]\nnums.sort(reverse=True)\nprint(nums)` | `reverse=True`를 넣으면 내림차순(큰 것부터)으로 정렬합니다. |
| 3 | `chars = ["c", "a", "b"]\nnew_chars = sorted(chars)\nprint(chars, new_chars)` | `sorted()`는 원본은 그대로 두고 정렬된 '새 리스트'를 반환합니다. |
| 4 | `vals = [10, 20, 30]\nvals.reverse()\nprint(vals)` | `reverse()`는 값을 정렬하지 않고 현재 순서만 그대로 뒤집습니다. |
| 5 | `names = ["Alice", "Bob", "Charlie"]\nnames.sort(key=len)\nprint(names)` | `key=len`을 지정하면 글자 길이를 기준으로 정렬합니다. |
| 6 | `data = ["10", "2", "30"]\ndata.sort()\nprint(data)` | 문자열 숫자는 맨 앞 글자 기준으로 정렬됨(10, 2, 30 순)에 주의하세요. |
| 7 | `data = [5, 2, 9, 1]\nprint(sorted(data)[::-1])` | 정렬한 뒤 슬라이싱으로 뒤집어 내림차순 효과를 냅니다. |
| 8 | `colors = ["red", "green", "blue"]\ncolors.sort()\nprint(" > ".join(colors))` | 색상 이름을 정렬한 뒤 세련되게 화살표로 연결해 출력합니다. |
| 9 | `n = [1, 2, 3]\nprint(list(reversed(n)))` | `reversed()` 함수로 이터레이터를 만든 뒤 리스트로 변환해 봅니다. |
| 10 | `prices = [1200, 800, 5000, 400]\nprices.sort()\nprint(f"Min: {prices[0]}, Max: {prices[-1]}")` | 정렬 후 첫 번째와 마지막 요소를 뽑아 최소/최대를 확인합니다. |
| 20 | `scores = [85, 92, 78, 100]\ns_scores = sorted(scores)\nprint(f"Rank-3: {s_scores[-3]}")` | [복습] 정렬된 리스트에서 뒤에서 3번째(점수가 3번째로 높은 것)를 뽑습니다. |
| * | *(세트 11-19 생략, 다양한 정렬 시나리오 연습)* | ... |

---

## 🟦 Lv 28: 숫자 리스트 통계 (Sum, Max, Min)
**설명**: 숫자들로 구성된 리스트의 합계, 최댓값, 최솟값을 한 번에 구하는 법을 익힙니다.

| Set | 코드 (code) | 가이드 (guide_ko) |
| :--- | :--- | :--- |
| 1 | `nums = [10, 20, 30]\nprint(sum(nums))` | `sum()`은 리스트의 모든 숫자를 더한 합계를 반환합니다. |
| 2 | `nums = [5, 12, 3, 9]\nprint(max(nums))\nprint(min(nums))` | 가장 큰 값과 가장 작은 값을 찾아줍니다. |
| 3 | `scores = [80, 90, 100]\navg = sum(scores) / len(scores)\nprint(f"AVG: {avg}")` | 합계를 개수로 나누어 평균을 구하는 가장 핵심적인 공식입니다. |
| 4 | `data = [1, 2, 3, 4, 5]\nprint(f"Range: {max(data) - min(data)}")` | 최댓값과 최솟값의 차이(범위)를 구합니다. |
| 5 | `prices = [12.5, 8.2, 15.0]\nprint(f"Total: ${sum(prices):.2f}")` | 실수 리스트의 합계를 구하고 소수점을 포맷팅합니다. |
| 6 | `nums = [4, 2, 8]\nprint(sum(nums) * 10)` | 합계에 특정 가중치를 곱하는 연산입니다. |
| 7 | `vals = [1, 2, 3]\nprint(max(vals) ** 2)` | 최댓값을 찾아 제곱해 봅니다. |
| 8 | `check = [True, False, True]\nprint(sum(check))` | 불리언 리스트에서 `sum`을 쓰면 `True`의 개수가 카운트됩니다. |
| 9 | `daily = [100, 200, 150]\nprint(f"Daily AVG: {int(sum(daily)/3)}")` | 일평균을 구한 뒤 소수점을 버리고 정수로 출력합니다. |
| 10 | `data = []\n# print(max(data)) # Error!` | 빈 리스트에서는 `max`, `min` 호출 시 에러가 나므로 주의해야 합니다. |
| 20 | `inv = [5, 10, 0, 8]\nprint(f"Total Stock: {sum(inv)}, Needs Refill: {min(inv) == 0}")` | [복습] 재고 합계를 구하고, 재고가 0인 품목이 있는지 판별합니다. |
| * | *(세트 11-19 생략, 리스트 통계 응용 연습)* | ... |

---

## 🟦 Lv 29: 중첩 리스트 (Nested Lists)
**설명**: 리스트 안에 또 다른 리스트를 넣어 2차원 공간(표나 행렬)을 표현하는 법을 익힙니다.

| Set | 코드 (code) | 가이드 (guide_ko) |
| :--- | :--- | :--- |
| 1 | `matrix = [[1, 2], [3, 4]]\nprint(matrix[0])\nprint(matrix[1])` | 리스트 내부의 각 리스트 요소에 접근해 봅니다. |
| 2 | `matrix = [[10, 20], [30, 40]]\nprint(matrix[0][1])` | 첫 번째 리스트의 두 번째 항목(`20`)을 가져옵니다. |
| 3 | `table = [["A", "B"], ["C", "D"]]\nprint(table[1][0])` | 두 번째 리스트의 첫 번째 항목(`C`)을 가져옵니다. |
| 4 | `groups = [[1, 2, 3], [4, 5, 6]]\nprint(len(groups))` | 바깥쪽 리스트의 길이를 재면 내부 리스트의 개수(2)가 나옵니다. |
| 5 | `groups = [[1, 2, 3], [4, 5, 6]]\nprint(len(groups[0]))` | 첫 번째 내부 리스트의 아이템 개수(3)를 확인합니다. |
| 6 | `data = [[1, 1], [2, 2]]\ndata[0][0] = 9\nprint(data)` | 특정 좌표(`[0][0]`)의 값을 직접 수정합니다. |
| 7 | `users = [["alice", 20], ["bob", 25]]\nprint(f"{users[0][0]} is {users[0][1]}yo")` | 이름과 나이가 짝지어진 데이터를 인덱스로 출력합니다. |
| 8 | `rows = [["#"] * 3] * 3\nprint(rows[0])` | 문자 반복을 활용해 같은 모양의 행을 리스트로 만듭니다. |
| 9 | `pixels = [[0, 0, 0], [255, 255, 255]]\nprint(f"Row 2 Start: {pixels[1][0]}")` | 이미지 픽셀 데이터 같은 중첩 구조를 연습합니다. |
| 10 | `inv = [["Apple", 5], ["Milk", 2]]\ninv[1][1] += 3\nprint(inv[1])` | 중첩 리스트 안의 수치를 연산하여 업데이트합니다. |
| 20 | `board = [["O", "X", "O"], ["X", "O", "X"]]\nprint(f"Center: {board[0][1]}, {board[1][1]}")` | [복습] 2D 게임판의 특정 위치 값들을 뽑아서 비교해 봅니다. |
| * | *(세트 11-19 생략, 다차원 데이터 처리 연습)* | ... |

---

## 🟦 Lv 30: [Challenge] 데이터 분석 기초 (Basic Analysis)
**설명**: [챌린지] 리스트의 메서드와 통계 함수를 결합하여 간단한 데이터 리포트를 작성합니다.

| Set | 코드 (code) | 가이드 (guide_ko) |
| :--- | :--- | :--- |
| 1 | `scores = [70, 85, 90, 75, 100]\nhi = max(scores)\nlo = min(scores)\nprint(f"Diff: {hi - lo}")` | 성적 리스트에서 최고점과 최저점의 차이를 구합니다. |
| 2 | `daily_steps = [8000, 12000, 7500, 10000]\navg = sum(daily_steps) / len(daily_steps)\nprint(f"Step AVG: {avg:.0f}")` | 일일 걸음 수의 평균을 소수점 없이 출력합니다. |
| 3 | `raw = [10, 5, 20, 15]\nfast_fix = sorted(raw, reverse=True)\nprint(f"Top 2: {fast_fix[:2]}")` | 데이터를 내림차순 정렬한 뒤 상위 2개만 뽑아냅니다. |
| 4 | `logs = ["OK", "ERR", "OK", "ERR", "ERR"]\nerr_cnt = logs.count("ERR")\nprint(f"Error Rate: {err_cnt / len(logs):.1%}")` | 에러 발생 로그를 분석하여 퍼센트 비율로 변환합니다. |
| 5 | `users = ["kim", "lee", "park", "kim"]\nprint(f"Total: {len(users)}, Unique Kim: {users.count('kim')}")` | 전체 명단 수와 특정 성씨의 빈도를 체크합니다. |
| 6 | `ids = [101, 105, 103, 102]\nids.sort()\nprint(f"Next ID: {ids[-1] + 1}")` | ID 리스트를 정렬한 뒤 마지막 번호에 1을 더해 새 ID를 추천합니다. |
| 7 | `data = [1, 2, 3, 4, 5, 6]\nmid = len(data) // 2\nprint(f"First half: {data[:mid]}")` | 리스트를 정확히 반으로 잘라 앞부분만 출력합니다. |
| 8 | `prices = [1200, 1500, 900]\ntaxed = sum(prices) * 1.1\nprint(f"Final Bill: {taxed:,.0f}")` | 가격 리스트의 합계에 세금을 적용하여 포맷팅합니다. |
| 9 | `tags = ["python", "java", "python", "php"]\nprint(f"Is Python popular? {tags.count('python') >= 2}")` | 특정 태그가 2번 이상 쓰였는지 인기 여부를 판별합니다. |
| 10 | `nums = [5, 1, 9, 3]\nnums.sort()\nmiddle_val = nums[1:3]\nprint(f"Middle Items: {middle_val}")` | 정렬 후 양 끝을 뺀 중간 데이터들만 발라냅니다. |
| 20 | `sales = [150, 200, 50, 300]\nsorted_sales = sorted(sales)\nprint(f"BEST: {sorted_sales[-1]}, WORST: {sorted_sales[0]}")` | [종합] 판매량 리스트에서 최고 실적과 최저 실적을 정렬 기법으로 찾습니다. |
| * | *(세트 11-19 생략, 데이터 핸들링 총괄 연습)* | ... |
