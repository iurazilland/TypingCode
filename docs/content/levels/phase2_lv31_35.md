# Phase 2: 리스트와 복합 자료구조 (Lv 31 - 35) 상세 세트

각 레벨은 20개의 연습 세트로 구성됩니다.

---

## 🟦 Lv 31: 딕셔너리 기초 (Dict Basics)
**설명**: 키(Key)와 값(Value)의 쌍으로 데이터를 이름표를 붙여 관리하는 법을 익힙니다.

| Set | 코드 (code) | 가이드 (guide_ko) |
| :--- | :--- | :--- |
| 1 | `user = {"name": "Alice", "age": 25}\nprint(user)` | 중괄호`{}`와 콜론`:`을 사용하여 딕셔너리를 만듭니다. |
| 2 | `user = {"name": "Alice", "age": 25}\nprint(user["name"])` | 대괄호 안에 키(Key)를 넣어 연결된 값(Value)을 가져옵니다. |
| 3 | `prices = {"apple": 1000, "milk": 2500}\nprint(prices["apple"])` | 상품명을 키로 사용하여 가격 정보를 조회합니다. |
| 4 | `book = {"title": "Python", "author": "Guido"}\nprint(book.get("title"))` | `get()` 메서드를 쓰면 존재하지 않는 키를 조회해도 에러 대신 None을 반환합니다. |
| 5 | `book = {"title": "Python"}\nprint(book.get("year", 2026))` | `get(key, default)` 형식을 쓰면 키가 없을 때의 기본값을 지정할 수 있습니다. |
| 6 | `data = {"A": 1, "B": 2}\nprint("A" in data)` | `in` 연산자로 특정 키(Key)가 딕셔너리에 존재하는지 확인합니다. |
| 7 | `empty = {}\nprint(type(empty))` | 중괄호만 비워두면 빈 딕셔너리가 생성됩니다. |
| 8 | `colors = {1: "Red", 2: "Blue"}\nprint(colors[1])` | 키값으로 문자열뿐만 아니라 숫자도 사용할 수 있습니다. |
| 9 | `person = {"id": 101, "name": "Kim"}\nprint(len(person))` | 딕셔너리의 길이는 키-값 쌍의 개수를 의미합니다. |
| 10 | `data = {"x": 10, "y": 20}\nprint(f"X is {data['x']}")` | f-string 안에서 딕셔너리 값을 사용할 때 따옴표 구분을 주의하세요. |
| 20 | `p = {"name": "Bot", "ver": 1.0}\nprint(f"SYSTEM: {p['name']} (v{p['ver']})")` | [복습] 딕셔너리에 담긴 정보를 정해진 형식으로 출력합니다. |
| * | *(세트 11-19 생략, 딕셔너리 구조 연습)* | ... |

---

## 🟦 Lv 32: 딕셔너리 수정과 삭제 (Update & Delete)
**설명**: 딕셔너리에 새 항목을 추가하거나 기존 값을 바꾸고 삭제하는 법을 익힙니다.

| Set | 코드 (code) | 가이드 (guide_ko) |
| :--- | :--- | :--- |
| 1 | `user = {"id": 1}\nuser["name"] = "Alice"\nprint(user)` | 존재하지 않는 키에 값을 대입하면 새로운 항목이 추가됩니다. |
| 2 | `user = {"id": 1, "name": "Old"}\nuser["name"] = "New"\nprint(user)` | 이미 존재하는 키에 값을 대입하면 기존 값이 업데이트됩니다. |
| 3 | `data = {"A": 1, "B": 2}\ndel data["A"]\nprint(data)` | `del` 키워드로 특정 키와 그에 연결된 값을 삭제합니다. |
| 4 | `data = {"A": 1, "B": 2}\nval = data.pop("B")\nprint(data, val)` | `pop(key)`은 항목을 삭제하면서 그 값을 반환합니다. |
| 5 | `data = {"name": "Kim"}\ndata.update({"age": 30, "city": "Seoul"})\nprint(data)` | `update()`는 다른 딕셔너리를 합쳐서 여러 항목을 한 번에 추가/수정합니다. |
| 6 | `stats = {"hp": 100}\nstats["hp"] -= 10\nprint(stats)` | 딕셔너리 내부의 수치 데이터도 복합 대입 연산자로 수정이 가능합니다. |
| 7 | `cart = {"Apple": 1}\ncart["Apple"] += 1\nprint(cart)` | 장바구니 수량을 1 늘리는 예제입니다. |
| 8 | `db = {"u1": "OK"}\ndb.popitem()\nprint(db)` | `popitem()`은 마지막에 추가된 항목을 삭제합니다. |
| 9 | `data = {"temp": 25}\ndata.clear()\nprint(data)` | 모든 항목을 지워서 빈 딕셔너리를 만듭니다. |
| 10 | `user = {"name": "Bob"}\nuser["grade"] = user.get("grade", "Basic")\nprint(user)` | 값이 없을 때만 기본값을 넣어 항목을 생성하는 패턴입니다. |
| 20 | `p = {"id": "dev"}\np["level"] = 5\np["id"] = p["id"].upper()\nprint(p)` | [복습] 항목을 추가하고 기존 키의 문자열 값을 가공하여 저장합니다. |
| * | *(세트 11-19 생략, 수정/삭제 변형 연습)* | ... |

---

## 🟦 Lv 33: 딕셔너리 메서드 활용 (Keys, Values, Items)
**설명**: 딕셔너리의 키들만 따로 모으거나 값들만 모아 처리하는 법을 익힙니다.

| Set | 코드 (code) | 가이드 (guide_ko) |
| :--- | :--- | :--- |
| 1 | `user = {"name": "Alice", "age": 25}\nprint(user.keys())` | `keys()`는 딕셔너리의 모든 키(Key) 목록을 가져옵니다. |
| 2 | `user = {"name": "Alice", "age": 25}\nprint(user.values())` | `values()`는 딕셔너리의 모든 값(Value) 목록을 가져옵니다. |
| 3 | `user = {"name": "Alice", "age": 25}\nprint(user.items())` | `items()`는 키와 값을 쌍(Tuple)으로 묶은 목록을 가져옵니다. |
| 4 | `d = {"A": 1, "B": 2}\nprint(list(d.keys()))` | 추출된 키 목록을 리스트로 변환하여 유연하게 사용할 수 있습니다. |
| 5 | `scores = {"Math": 90, "Eng": 80}\nprint(sum(scores.values()))` | 값들만 모아서 합계를 구하는 통계 작업에 활용합니다. |
| 6 | `data = {"a": 1, "b": 2}\nprint("a" in data.keys())` | 특정 키가 있는지 확인할 때 명시적으로 `.keys()`를 쓸 수도 있습니다. |
| 7 | `user = {"id": "001"}\nprint(len(user.items()))` | `items`의 개수를 세어 데이터 저장소의 크기를 확인합니다. |
| 8 | `d = {"A": 10, "B": 20}\nprint(max(d.values()))` | 저장된 값들 중 가장 큰 수치를 찾습니다. |
| 9 | `d = {"apple": "red", "banana": "yellow"}\nprint("red" in d.values())` | 특정 값이 들어있는지 확인할 때 유용합니다. |
| 10 | `user = {"name": "Alice"}\nprint(", ".join(user.keys()))` | 모든 키를 콤마로 연결하여 하나의 문자열로 만듭니다. |
| 20 | `s = {"k1": 10, "k2": 20}\nall_v = list(s.values())\nprint(f"Total: {sum(all_v)}, Items: {len(all_v)}")` | [복습] 값들을 리스트로 뽑아 합계와 개수를 함께 출력합니다. |
| * | *(세트 11-19 생략, 데이터 집계 연습)* | ... |

---

## 🟦 Lv 34: 중첩 딕셔너리 (Nested Dicts)
**설명**: 딕셔너리 안에 또 다른 딕셔너리나 리스트를 넣어 복잡한 데이터를 구조화합니다.

| Set | 코드 (code) | 가이드 (guide_ko) |
| :--- | :--- | :--- |
| 1 | `users = {"u1": {"name": "Alice"}, "u2": {"name": "Bob"}}` | 딕셔너리 안에 하위 딕셔너리를 포함시킵니다. |
| 2 | `data = {"user": {"id": 1, "name": "A"}}\nprint(data["user"]["name"])` | 대괄호를 두 번 사용하여 깊은 곳의 값에 접근합니다. |
| 3 | `stats = {"player": {"hp": 100, "mp": 50}}\nstats["player"]["hp"] -= 20\nprint(stats["player"])` | 중첩된 구조 내부의 수치를 직접 수정합니다. |
| 4 | `course = {"students": ["Alice", "Bob", "Charlie"]}\nprint(course["students"][0])` | 딕셔너리의 값이 리스트인 형태에서 특정 요소를 꺼내옵니다. |
| 5 | `sys = {"nodes": [{"id": 1}, {"id": 2}]}\nprint(sys["nodes"][1]["id"])` | 리스트 안에 딕셔너리가 들어있는 매우 실무적인 데이터 구조입니다. |
| 6 | `db = {"users": {}}\ndb["users"]["admin"] = "pass123"\nprint(db)` | 빈 중첩 딕셔너리를 생성한 뒤 동적으로 키를 추가합니다. |
| 7 | `meta = {"ver": "1.0", "info": {"author": "Ant"}}\nprint(meta.get("info").get("author"))` | `get()`을 연속 사용하여 안전하게 중첩 데이터를 조회합니다. |
| 8 | `item = {"name": "P1", "specs": {"color": "Red"}}\nprint(f"Color: {item['specs']['color']}")` | 하위 속성 값을 f-string에 담아 출력합니다. |
| 9 | `p = {"tags": ["A", "B"], "data": {"v": 10}}\nprint(len(p["tags"]), p["data"]["v"])` | 리스트와 딕셔너리가 혼합된 메타데이터 구조를 연습합니다. |
| 10 | `data = {"a": {"b": {"c": 1}}}\nprint(data["a"]["b"]["c"])` | 3단계 이상 중첩된 구조의 데이터 탐색 연습입니다. |
| 20 | `user = {"id": "tester", "scores": {"math": 90, "eng": 85}}\nprint(f"Total: {sum(user['scores'].values())}")` | [복습] 중첩된 딕셔너리 내부의 점수들의 합계를 계산합니다. |
| * | *(세트 11-19 생략, 계층적 데이터 처리 연습)* | ... |

---

## 🟦 Lv 35: [Challenge] 유저 프로필 관리자
**설명**: [챌린지] 딕셔너리의 중첩 구조와 메서드를 활용해 회원 정보를 관리하는 시뮬레이션을 합니다.

| Set | 코드 (code) | 가이드 (guide_ko) |
| :--- | :--- | :--- |
| 1 | `user = {"id": "code_master", "profile": {"name": "Alice", "age": 25}}` | 기본 유저 정보를 중첩 딕셔너리로 선언합니다. |
| 2 | `user["stats"] = {"level": 5, "exp": 1200}\nprint(user.keys())` | 유저의 스탯(Stats) 정보를 신규 추가합니다. |
| 3 | `user["stats"]["exp"] += 500\nprint(f"EXP Updated: {user['stats']['exp']}")` | 중첩된 경험치 데이터를 갱신합니다. |
| 4 | `user["tags"] = ["Python", "C++"]\nuser["tags"].append("Javascript")\nprint(user["tags"])` | 유저의 관심 키워드 리스트를 관리합니다. |
| 5 | `del user["profile"]["age"]\nprint("age" in user["profile"])` | 특정 개인 정보 항목을 삭제 처리합니다. |
| 6 | `settings = {"theme": "dark", "zoom": 1.2}\nuser.update({"config": settings})\nprint(user["config"])` | 설정 테마 정보를 딕셔너리로 묶어 유저 데이터와 병합합니다. |
| 7 | `print(f"Total Sections: {len(user)}")` | 유저 데이터 내의 대분류(Profile, Stats, Tags 등) 개수를 셉니다. |
| 8 | `skills = ", ".join(user["tags"])\nprint(f"Skill Set: {skills.upper()}")` | 태그 리스트를 합쳐서 가독성 좋게 보여줍니다. |
| 9 | `user["id"] = "@" + user["id"]\nprint(f"Handle: {user['id']}")` | 유저 ID 앞에 골뱅이(@) 기호를 붙여 수정합니다. |
| 10 | `data_keys = list(user.keys())\nprint(f"Data Root: {data_keys}")` | 유저 데이터의 루트 키 목록을 리스트로 확인합니다. |
| 20 | `final = {"user": user, "status": "active"}\nprint(f"USER: {final['user']['id']} STATUS: {final['status'].upper()}")` | [종합] 유저 객체와 시스템 상태를 결합한 최종 관리 구조를 만듭니다. |
| * | *(세트 11-19 생략, 프로필 엔진 응용 연습)* | ... |
