# Phase 2: 리스트와 복합 자료구조 (Lv 36 - 40) 상세 세트

각 레벨은 20개의 연습 세트로 구성됩니다.

---

## 🟦 Lv 36: 튜플과 언패킹 (Tuples & Unpacking)
**설명**: 한 번 정하면 바꿀 수 없는 데이터 묶음(Tuple)과 이를 여러 변수로 나눠 담는 법을 익힙니다.

| Set | 코드 (code) | 가이드 (guide_ko) |
| :--- | :--- | :--- |
| 1 | `point = (10, 20)\nprint(point)` | 소괄호`()`를 사용하여 튜플을 만듭니다. |
| 2 | `t = (1, 2, 3)\nprint(t[0], t[-1])` | 튜플도 리스트처럼 인덱싱과 슬라이싱이 가능합니다. |
| 3 | `t = (1, 2)\n# t[0] = 99 # Error!` | 튜플은 한 번 생성하면 내부 값을 변경할 수 없습니다(Immutable). |
| 4 | `x, y = (10, 20)\nprint(x, y)` | 튜플의 요소를 각각의 변수에 나누어 담는 것을 '언패킹'이라고 합니다. |
| 5 | `a, b = 1, 2\na, b = b, a\nprint(a, b)` | 튜플 패킹/언패킹을 이용해 두 변수의 값을 아주 쉽게 바꿀 수 있습니다. |
| 6 | `single = (10,)\nprint(type(single))` | 요소가 하나뿐인 튜플은 숫자 뒤에 반드시 콤마`,`를 찍어야 합니다. |
| 7 | `t = (1, 2, 3)\nprint(len(t))` | `len()`으로 튜플의 항목 개수를 확인합니다. |
| 8 | `t = (1, 2) + (3, 4)\nprint(t)` | 두 튜플을 더해서 새로운 튜플을 만들 수 있습니다. |
| 9 | `t = ("A",) * 3\nprint(t)` | 튜플에 숫자를 곱해 반복된 튜플을 생성합니다. |
| 10 | `data = (10, 20, 30, 40)\na, *b = data\nprint(a, b)` | `*`를 사용하면 남은 요소를 리스트로 몰아서 담을 수 있습니다. |
| 20 | `user = ("alice", "admin", 2026)\nname, role, year = user\nprint(f"{name} ({role}) joined in {year}")` | [복습] 유저 정보를 튜플로 관리하고 언패킹하여 문장을 구성합니다. |
| * | *(세트 11-19 생략, 튜플 언패킹 연습)* | ... |

---

## 🟦 Lv 37: 셋(Set) 기초와 중복 제거
**설명**: 중복을 허용하지 않고 순서가 없는 데이터 집합(Set)을 익힙니다.

| Set | 코드 (code) | 가이드 (guide_ko) |
| :--- | :--- | :--- |
| 1 | `s = {1, 2, 3, 2, 1}\nprint(s)` | 셋(`{}`)은 중복된 값을 자동으로 제거하고 유일한 값만 남깁니다. |
| 2 | `s = set([1, 2, 2, 3])\nprint(s)` | 리스트를 `set()`으로 감싸면 중복 요소를 말끔히 지울 수 있습니다. |
| 3 | `s = {1, 2}\ns.add(3)\nprint(s)` | `add()` 메서드로 새로운 요소를 추가합니다. |
| 4 | `s = {1, 2, 3}\ns.remove(2)\nprint(s)` | `remove()`로 특정 요소를 삭제합니다. (없으면 에러) |
| 5 | `s = {1, 2, 3}\ns.discard(99)\nprint(s)` | `discard()`는 삭제하려는 값이 없어도 에러가 나지 않아 안전합니다. |
| 6 | `s = set("hello")\nprint(s)` | 문자열을 셋으로 만들면 사용된 철자의 종류(중복 제거)를 알 수 있습니다. |
| 7 | `s = {1, 2, 3}\nprint(2 in s)` | 특정 요소가 집합 안에 존재하는지 매우 빠르게 확인합니다. |
| 8 | `s = {1, 2}\ns.update([3, 4, 5])\nprint(s)` | `update()`를 쓰면 여러 개의 요소를 한 번에 추가합니다. |
| 9 | `s = {1, 2, 3}\nitem = s.pop()\nprint(s, item)` | 셋은 순서가 없으므로 `pop()`을 하면 임의의 요소가 나옵니다. |
| 10 | `s = {1, 2, 3}\ns.clear()\nprint(s)` | 집합의 모든 내용을 비웁니다. |
| 20 | `raw = ["A", "B", "A", "C"]\nunique = list(set(raw))\nunique.sort()\nprint(unique)` | [복습] 리스트의 중복을 제거한 뒤 다시 정렬된 리스트로 변환합니다. |
| * | *(세트 11-19 생략, 집합 기본 동작 연습)* | ... |

---

## 🟦 Lv 38: 집합 연산 (Set Operations)
**설명**: 합집합, 교집합, 차집합 등 수학적인 집합 계산을 수행합니다.

| Set | 코드 (code) | 가이드 (guide_ko) |
| :--- | :--- | :--- |
| 1 | `a = {1, 2, 3}\nb = {3, 4, 5}\nprint(a | b)` | `|` (Vertical Bar) 기호는 두 집합의 **합집합**을 구합니다. |
| 2 | `a = {1, 2, 3}\nb = {3, 4, 5}\nprint(a & b)` | `&` (Ampersand) 기호는 공통된 요소인 **교집합**을 구합니다. |
| 3 | `a = {1, 2, 3}\nb = {3, 4, 5}\nprint(a - b)` | `-` 기호는 한쪽 집합에만 있는 **차집합**을 구합니다. |
| 4 | `a = {1, 2, 3}\nb = {3, 4, 5}\nprint(a ^ b)` | `^` 기호는 합집합에서 교집합 부분을 뺀 **대칭 차집합**을 구합니다. |
| 5 | `a = {1, 2}\nprint(a.union({3, 4}))` | 기호 대신 `union()` 메서드를 사용할 수도 있습니다. |
| 6 | `a = {1, 2, 3}\nprint(a.intersection({2, 3, 4}))` | 메서드로 교집합을 구하는 예제입니다. |
| 7 | `a = {1, 2}\nprint({1}.issubset(a))` | `{1}`이 집합 `a`의 내부 요소(부분집합)인지 확인합니다. |
| 8 | `a = {1, 2}\nprint(a.issuperset({1}))` | `a`가 `{1}`을 포함하는 부모 집합인지 확인합니다. |
| 9 | `a = {1, 2}\nb = {3, 4}\nprint(a.isdisjoint(b))` | 두 집합이 서로 겹치는 요소가 하나도 없는지 확인합니다. |
| 10 | `a = {1, 2}\na |= {3, 4}\nprint(a)` | 합집합 결과를 기존 집합에 바로 반영(`|=`)합니다. |
| 20 | `math = {"A", "B", "C"}\neng = {"B", "D"}\nprint(f"Both: {math & eng}, Total: {len(math | eng)}")` | [복습] 과목별 수강생 셋을 이용해 중복 수강생과 전체 인원수를 구합니다. |
| * | *(세트 11-19 생략, 수학 집합 응용 연습)* | ... |

---

## 🟦 Lv 39: 자료구조 간 변환 (Type Conversion)
**설명**: 리스트, 튜플, 셋 간의 자유로운 형 변환을 통해 상황에 맞는 데이터 처리를 합니다.

| Set | 코드 (code) | 가이드 (guide_ko) |
| :--- | :--- | :--- |
| 1 | `lst = [1, 2, 3]\ntpl = tuple(lst)\nprint(tpl)` | 리스트를 수정 불가능한 튜플로 변환합니다. |
| 2 | `tpl = (10, 20)\nlst = list(tpl)\nlst.append(30)\nprint(lst)` | 튜플을 리스트로 바꿔서 내용을 추가한 후 사용합니다. |
| 3 | `lst = [1, 1, 2, 3, 3]\ns = set(lst)\nprint(s)` | 리스트를 셋으로 바꿔 중복을 제거합니다. |
| 4 | `s = {1, 2, 3}\ntpl = tuple(s)\nprint(tpl)` | 셋을 튜플로 변환합니다. |
| 5 | `d = {"a": 1, "b": 2}\nprint(list(d.keys()))` | 딕셔너리의 키들만 뽑아 리스트로 만듭니다. |
| 6 | `d = {"a": 1, "b": 2}\nprint(list(d.items()))` | 딕셔너리의 키-값 쌍을 튜플들이 담긴 리스트로 변환합니다. |
| 7 | `s = set("python")\nlst = sorted(list(s))\nprint(lst)` | 문자열을 셋으로 바꿔 중복 제거 후, 리스트화하여 정렬까지 수행합니다. |
| 8 | `keys = ["id", "pw"]\nvals = ["admin", "123"]\nd = dict(zip(keys, vals))\nprint(d)` | 두 개의 리스트를 `zip`하여 딕셔너리로 합칩니다. |
| 9 | `names = ("a", "b", "c")\nprint("-".join(names))` | 튜플에 담긴 문자열들도 `join`으로 합칠 수 있습니다. |
| 10 | `data = [(1, 2), (3, 4)]\nprint(data[0][1])` | 튜플들을 담고 있는 리스트(리스트 오브 튜플) 구조를 연습합니다. |
| 20 | `p = set([1, 2, 3])\np.add(4)\nprint(tuple(sorted(list(p))))` | [복습] 셋에 데이터를 추가하고 정려된 튜플 형태로 최종 변환합니다. |
| * | *(세트 11-19 생략, 복합 자료형 변환 연습)* | ... |

---

## 🟦 Lv 40: [Challenge] 중복 ID 로그 클리너
**설명**: [챌린지] 셋의 중복 제거 특성과 집합 연산을 사용하여 데이터 로그를 정제합니다.

| Set | 코드 (code) | 가이드 (guide_ko) |
| :--- | :--- | :--- |
| 1 | `logs = ["user1", "user2", "user1", "user3"]\nunique_users = set(logs)\nprint(f"Unique: {len(unique_users)}")` | 전체 접속 로그에서 중복을 제외한 실제 유저 수를 구합니다. |
| 2 | `day1 = {"a", "b", "c"}\nday2 = {"b", "c", "d"}\nprint(f"New Users: {day2 - day1}")` | 1일차와 2일차 유저를 대조하여 2일차에 처음 나타난 신규 유저를 찾습니다. |
| 3 | `admins = {"root", "admin"}\nusers = {"admin", "guest", "user1"}\nprint(f"Admin Access: {admins & users}")` | 전체 접속자 중 관리자 권한을 가진 사람만 골라냅니다. |
| 4 | `all_staff = day1 | day2\nprint(f"Total Staff Pool: {all_staff}")` | 이틀간 나타난 모든 유저를 합친 전체 명단을 만듭니다. |
| 5 | `invalid = {"test", "guest"}\ncurrent = {"admin", "test", "user1"}\nprint(f"Valid: {current - invalid}")` | 현재 명단에서 테스트용 계정이나 게스트를 제외합니다. |
| 6 | `data = ("apple", "banana", "apple")\nclean = sorted(list(set(data)))\nprint(f"Sorted Unique: {clean}")` | 튜플 데이터의 중복을 제거하고 정렬된 리스트로 정제합니다. |
| 7 | `s1 = {1, 2, 3}\ns2 = {4, 5, 6}\nprint(f"Common items exist? {not s1.isdisjoint(s2)}")` | 두 집합 간에 겹치는 요소가 있는지 판별합니다. |
| 8 | `input_tags = "python, java, python, c++"\ntags = set(input_tags.split(", "))\nprint(f"Tags: {', '.join(sorted(tags))}")` | 콤마로 구분된 태그들을 쪼갠 뒤 중복을 없애고 예쁘게 합칩니다. |
| 9 | `v = ("key", "val")\nd = {v[0]: v[1]}\nprint(d)` | 튜플의 데이터를 이용해 딕셔너리의 항목을 구성해 봅니다. |
| 10 | `s = {10, 20, 30}\ns.update({30, 40, 50})\nprint(f"Final Count: {len(s)}")` | 기존 집합에 새로운 집합 데이터를 병합하여 항목 수를 확인합니다. |
| 20 | `grp_a = {"x", "y"}\ngrp_b = {"y", "z"}\nprint(f"Exclusive to each: {grp_a ^ grp_b}")` | [종합] 두 그룹 중 서로 겹치지 않고 오직 한쪽에만 있는 멤버들을 찾아냅니다. |
| * | *(세트 11-19 생략, 집합 계산 및 데이터 정제 시뮬레이션)* | ... |
