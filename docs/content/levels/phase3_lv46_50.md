# Phase 3: 제어문과 프로그램 흐름 (Lv 46 - 50) 상세 세트

각 레벨은 20개의 연습 세트로 구성됩니다.

---

## 🟦 Lv 46: For 반복문과 range 기초
**설명**: `range()` 함수를 사용하여 지정한 횟수만큼 코드를 반복 실행하는 법을 익힙니다.

| Set | 코드 (code) | 가이드 (guide_ko) |
| :--- | :--- | :--- |
| 1 | `for i in range(3):\n    print("Hello")` | 3번 반복하여 "Hello"를 출력합니다. |
| 2 | `for i in range(5):\n    print(i)` | `range(5)`는 0부터 4까지의 숫자를 하나씩 꺼내줍니다. |
| 3 | `for i in range(1, 6):\n    print(f"Step {i}")` | 1부터 5까지(`1~6 전까지`) 범위를 지정해 반복합니다. |
| 4 | `for i in range(0, 10, 2):\n    print(i)` | 0부터 10 전까지 2씩 건너뛰며 출력합니다 (0, 2, 4, 6, 8). |
| 5 | `for i in range(5, 0, -1):\n    print(i)` | 5부터 1까지 뒤로 하나씩 세는 카운트다운을 구현합니다. |
| 6 | `for _ in range(3):\n    print("---")` | 변수 `i`를 쓰지 않을 때는 언더바(`_`)를 쓰는 관습이 있습니다. |
| 7 | `total = 0\nfor i in range(1, 11):\n    total += i\nprint(total)` | 1부터 10까지의 합계를 반복문으로 구합니다. |
| 8 | `for i in range(3):\n    print("Repeat", i + 1)` | 인덱스 숫자에 1을 더해 회차 정보를 출력합니다. |
| 9 | `for i in range(10):\n    if i % 3 == 0:\n        print(i)` | 0~9 중 3의 배수만 골라 출력합니다. |
| 10 | `for i in range(1, 4):\n    print("*" * i)` | 별찍기: 줄 번호만큼 별 개수를 늘려가며 출력합니다. |
| 20 | `limit = 5\nfor i in range(limit):\n    print(f"Index {i} of {limit-1}")` | [복습] 변수로 범위를 정해 인덱스 진행 상황을 출력합니다. |
| * | *(세트 11-19 생략, range 활용 다양한 반복)* | ... |

---

## 🟦 Lv 47: 리스트 순회 (Iterating Lists)
**설명**: 리스트에 담긴 개별 데이터들을 하나씩 꺼내어 처리하는 법을 익힙니다.

| Set | 코드 (code) | 가이드 (guide_ko) |
| :--- | :--- | :--- |
| 1 | `fruits = ["🍎", "🍊", "🍇"]\nfor f in fruits:\n    print(f)` | 리스트의 요소를 하나씩 꺼내어 변수 `f`에 담아 출력합니다. |
| 2 | `scores = [80, 90, 70]\nfor s in scores:\n    print(f"Value: {s}")` | 숫자 리스트를 돌며 점수를 하나씩 확인합니다. |
| 3 | `names = ["alice", "bob"]\nfor n in names:\n    print(n.upper())` | 리스트를 돌며 문자열 데이터를 대문자로 가공해 출력합니다. |
| 4 | `nums = [1, 2, 3, 4, 5]\nfor n in nums:\n    if n % 2 == 0:\n        print(n)` | 리스트 안에서 짝수만 골라 출력합니다. |
| 5 | `total = 0\nfor n in [10, 20, 30]:\n    total += n\nprint(total)` | 리스트 요소들의 총합을 반복문으로 계산합니다. |
| 6 | `words = ["Py", "thon"]\nres = ""\nfor w in words:\n    res += w\nprint(res)` | 리스트의 문자열들을 하나로 이어붙입니다. |
| 7 | `data = [3, -1, 5, -2]\nfor d in data:\n    print(abs(d))` | 리스트 안의 숫자를 모두 절대값으로 바꿔 출력합니다. |
| 8 | `colors = ["red", "blue"]\nfor i, c in enumerate(colors):\n    print(f"#{i}: {c}")` | `enumerate`를 쓰면 인덱스 번호와 실제 값을 동시에 가져옵니다. |
| 9 | `prices = [100, 200]\nfor p in prices:\n    print(f"With Tax: {p * 1.1}")` | 전 품목에 세금을 적용한 가격을 한 번에 보여줍니다. |
| 10 | `tags = ["#dev", "#code"]\nfor t in tags:\n    print(t.strip("#"))` | 태그에서 해시 기호를 제거한 알맹이만 출력합니다. |
| 20 | `user_list = ["admin", "guest", "test"]\nfor user in user_list:\n    if user != "test":\n        print(f"Active: {user}")` | [복습] 예외(test 유저)를 제외한 나머지 사용자만 활성화 메시지를 띄웁니다. |
| * | *(세트 11-19 생략, 리스트 순회 및 가공 연습)* | ... |

---

## 🟦 Lv 48: 딕셔너리 순회 (Iterating Dicts)
**설명**: 딕셔너리의 키와 값을 반복하여 조회하고 처리하는 법을 익힙니다.

| Set | 코드 (code) | 가이드 (guide_ko) |
| :--- | :--- | :--- |
| 1 | `data = {"A": 1, "B": 2}\nfor k in data:\n    print(k)` | 기본적으로 `for` 문을 돌리면 키(Key)값들이 순차적으로 나옵니다. |
| 2 | `data = {"A": 1, "B": 2}\nfor v in data.values():\n    print(v)` | `.values()`를 사용하여 값(Value)들만 반복합니다. |
| 3 | `data = {"A": 1, "B": 2}\nfor k, v in data.items():\n    print(k, ":", v)` | `.items()`를 사용하면 키와 값을 동시에 꺼내어 쓸 수 있습니다. |
| 4 | `inv = {"Apple": 5, "Milk": 3}\nfor item in inv:\n    print(f"{item} is in stock.")` | 재고가 있는 품목들을 하나씩 호명합니다. |
| 5 | `scores = {"Math": 90, "Eng": 85}\nfor s in scores.values():\n    if s >= 90:\n        print("Excellent!")` | 점수들 중 기준 이상인 경우 격려 문구를 띄웁니다. |
| 6 | `db = {"u1": "OK", "u2": "FAIL"}\nfor u, s in db.items():\n    print(f"User {u} state is {s}")` | 유저와 해당 상태를 매칭하여 깔끔하게 문장으로 출력합니다. |
| 7 | `cart = {"P1": 1200, "P2": 800}\ntotal = 0\nfor p in cart.values():\n    total += p\nprint(total)` | 딕셔너리에 담긴 가격들의 총합을 구합니다. |
| 8 | `colors = {"red": "#F00", "blue": "#00F"}\nfor c in colors:\n    print(f"Color {c} hex: {colors[c]}")` | 키를 이용해 변수 인덱싱 방식으로 값을 조회하며 반복합니다. |
| 9 | `data = {"x": 10, "y": 20}\nres = {}\nfor k, v in data.items():\n    res[k] = v * 2\nprint(res)` | 기존 데이터를 가공하여 새로운 딕셔너리를 만드는 반복문입니다. |
| 10 | `p = {"name": "Bob", "role": "User"}\nfor k in p:\n    print(f"Field: {k.upper()}")` | 저장된 속성(필드)들의 이름을 대문자로 확인합니다. |
| 20 | `user = {"Math": 95, "Eng": 80}\nfor sub, score in user.items():\n    status = "Pass" if score >= 90 else "Fail"\n    print(f"{sub}: {status}")` | [복습] 과목명과 점수를 받아 합격 여부를 판별해 출력합니다. |
| * | *(세트 11-19 생략, 딕셔너리 키/값 처리 연습)* | ... |

---

## 🟦 Lv 49: 중첩 반복문 기초 (Nested Loops)
**설명**: 반복문 안에 또 다른 반복문이 들어있는 구조를 익힙니다. (구구단, 격자무늬 등)

| Set | 코드 (code) | 가이드 (guide_ko) |
| :--- | :--- | :--- |
| 1 | `for i in range(2):\n    for j in range(2):\n        print(f"i:{i}, j:{j}")` | 바깥쪽 반복 한 번에 안쪽 반복이 전체 실행되는 것을 확인합니다. |
| 2 | `for i in [1, 2]:\n    for char in ["A", "B"]:\n        print(i, char)` | 숫자와 문자를 조합하여 가능한 쌍을 모두 출력합니다. |
| 3 | `for i in range(1, 3):\n    for j in range(1, 3):\n        print(f"{i} * {j} = {i*j}")` | 2단까지만 출력하는 아주 작은 구구단 예제입니다. |
| 4 | `matrix = [[1, 2], [3, 4]]\nfor row in matrix:\n    for val in row:\n        print(val, end=" ")\n    print()` | 2차원 리스트(표)를 순회하며 내용을 정렬해 출력합니다. |
| 5 | `for i in range(3):\n    for j in range(i + 1):\n        print("*", end="")\n    print()` | 바깥쪽 인덱스 `i`를 안쪽 반복의 범위로 활용해 별을 늘려갑니다. |
| 6 | `groups = [["A", "B"], ["C", "D"]]\nfor g in groups:\n    print("Group Start")\n    for m in g:\n        print("-", m)` | 그룹별 멤버 리스트를 계층적으로 출력합니다. |
| 7 | `for i in range(2):\n    line = ""\n    for j in range(3):\n        line += "#"\n    print(line)` | 안쪽 반복에서 문자열을 완성한 뒤 바깥쪽에서 출력해 봅니다. |
| 8 | `data = [[10], [20, 30]]\nfor sub in data:\n    for v in sub:\n        print(v)` | 내부 리스트의 크기가 달라도 문제없이 모두 순회합니다. |
| 9 | `for x in range(2):\n    for y in range(2):\n        if x == y:\n            print(f"Match: {x}")` | 중첩 반복 도중 조건이 맞을 때만 특별한 액션을 수행합니다. |
| 10 | `codes = ["A", "B"]\nnums = [1, 2]\nfor c in codes:\n    for n in nums:\n        print(c + str(n), end=" ")` | "A1 A2 B1 B2" 와 같이 코드 이름을 생성하는 로직입니다. |
| 20 | `for i in range(3):\n    print(f"Floor {i}:", end=" ")\n    for j in range(2):\n        print(f"Room {j}", end=" ")\n    print()` | [복습] 층과 호수로 구성된 건물 구조를 중첩 반복으로 출력합니다. |
| * | *(세트 11-19 생략, 중첩 구조 응용 연습)* | ... |

---

## 🟦 Lv 50: [Challenge] 데이터 집계기 (Aggregator)
**설명**: [챌린지] 반복문과 조건을 결합하여 복잡한 데이터를 수집하고 결과를 요약합니다.

| Set | 코드 (code) | 가이드 (guide_ko) |
| :--- | :--- | :--- |
| 1 | `nums = [1, 2, 3, 4, 5, 6]\neven_count = 0\nfor n in nums:\n    if n % 2 == 0:\n        even_count += 1\nprint(f"Evens: {even_count}")` | 리스트를 돌며 짝수가 몇 개인지 카운트합니다. |
| 2 | `prices = [100, 500, 200, 800]\ntotal = 0\nfor p in prices:\n    if p >= 500:\n        total += p\nprint(f"Premium Total: {total}")` | 500원 이상의 비싼 물건들의 합계만 따로 구합니다. |
| 3 | `users = ["alice", "bob", "alex", "john"]\na_count = 0\nfor u in users:\n    if u.startswith("a"):\n        a_count += 1\nprint(f"A-Names: {a_count}")` | 이름이 'a'로 시작하는 사람의 수를 집계합니다. |
| 4 | `data = [10, 55, 30, 80]\nres = []\nfor d in data:\n    if d > 40:\n        res.append("High")\n    else:\n        res.append("Low")\nprint(res)` | 원본 데이터를 조건에 따라 "High/Low" 문구로 변환하여 새 리스트에 담습니다. |
| 5 | `inventory = {"Apple": 10, "Milk": 0, "Eggs": 5}\nfor item, qty in inventory.items():\n    if qty == 0:\n        print(f"REFILL: {item}")` | 재고가 0인 품목만 골라내어 보충 알림을 띄웁니다. |
| 6 | `nums = [1, 5, 2, 8, 3]\nmax_val = nums[0]\nfor n in nums:\n    if n > max_val:\n        max_val = n\nprint(f"Search Max: {max_val}")` | `max()` 함수 없이 반복문으로 가장 큰 수를 찾는 알고리즘 연습입니다. |
| 7 | `words = ["python", "is", "awesome"]\ntotal_len = 0\nfor w in words:\n    total_len += len(w)\nprint(f"Total Chars: {total_len}")` | 리스트 안의 모든 단어의 글자 수를 합산합니다. |
| 8 | `logs = ["INFO", "ERROR", "INFO"]\nerr_list = []\nfor i, msg in enumerate(logs):\n    if msg == "ERROR":\n        err_list.append(i)\nprint(f"Error Indexes: {err_list}")` | 에러가 발생한 지점의 번호(인덱스)들만 모아서 출력합니다. |
| 9 | `matrix = [[1, 2, 3], [4, 5, 6]]\ntotal = 0\nfor row in matrix:\n    total += sum(row)\nprint(f"All Sum: {total}")` | 중첩 리스트의 모든 숫자를 합산하는 최적화 로직입니다. |
| 20 | `names = ["Alice", "Bob", "Alice"]\nd = {}\nfor n in names:\n    d[n] = d.get(n, 0) + 1\nprint(f"Count Report: {d}")` | [종합] 이름의 출현 빈도를 딕셔너리로 깔끔하게 집계하여 출력합니다. |
| * | *(세트 11-19 생략, 실제 데이터 분석 시나리오 연습)* | ... |
