# Phase 3: 제어문과 프로그램 흐름 (Lv 56 - 60) 상세 세트

각 레벨은 20개의 연습 세트로 구성됩니다.

---

## 🟦 Lv 56: 리스트 컴프리헨션 기초 (List Comprehension)
**설명**: 코드를 한 줄로 줄여서 새로운 리스트를 생성하는 파이썬의 핵심 문법을 익힙니다.

| Set | 코드 (code) | 가이드 (guide_ko) |
| :--- | :--- | :--- |
| 1 | `nums = [i for i in range(5)]\nprint(nums)` | 0부터 4까지 들어있는 리스트를 한 줄로 만듭니다. |
| 2 | `sq = [i**2 for i in range(1, 4)]\nprint(sq)` | 1, 2, 3의 제곱수들이 담긴 리스트를 생성합니다. |
| 3 | `txt = [s.upper() for s in ["a", "b"]]\nprint(txt)` | 리스트의 모든 문자를 대문자로 바꿔 새 리스트를 만듭니다. |
| 4 | `vals = [i * 10 for i in range(1, 4)]\nprint(vals)` | 각 숫자에 10을 곱한 결과로 리스트를 채웁니다. |
| 5 | `chars = [c for c in "Hi"]\nprint(chars)` | 문자열 "Hi"를 한 글자씩 쪼개 리스트로 변환합니다. |
| 6 | `nums = [i + 5 for i in [1, 2, 3]]\nprint(nums)` | 기존 리스트 값들에 5를 더해 새 리스트를 만듭니다. |
| 7 | `same = [10 for _ in range(3)]\nprint(same)` | 같은 값(10)을 3번 반복해서 채운 리스트 예제입니다. |
| 8 | `rev = [i for i in range(3, 0, -1)]\nprint(rev)` | 거꾸로 세는 숫자로 리스트를 채웁니다. |
| 9 | `prices = [int(p) for p in ["10", "20"]]\nprint(prices)` | 문자열 숫자 리스트를 정수형 리스트로 한 번에 변환합니다. |
| 10 | `f = [float(i) for i in range(3)]\nprint(f)` | 정수 연속을 실수형 리스트로 변환하여 생성합니다. |
| 20 | `labels = [f"Rank-{i}" for i in range(1, 4)]\nprint(labels)` | [복습] 1, 2, 3 번호를 활용해 "Rank-1" 등의 이름을 생성해 담습니다. |
| * | *(세트 11-19 생략, 기본 컴프리헨션 연습)* | ... |

---

## 🟦 Lv 57: 필터링이 포함된 컴프리헨션 (Comprehension with If)
**설명**: 조건문을 섞어서 원하는 데이터만 골라 리스트로 만드는 법을 익힙니다.

| Set | 코드 (code) | 가이드 (guide_ko) |
| :--- | :--- | :--- |
| 1 | `even = [i for i in range(10) if i % 2 == 0]\nprint(even)` | 0~9 중 짝수만 골라 리스트를 생성합니다. |
| 2 | `over = [n for n in [10, 50, 40] if n > 30]\nprint(over)` | 리스트에서 30보다 큰 값만 걸러냅니다. |
| 3 | `words = ["apple", "go", "banana"]\nlong = [w for w in words if len(w) > 4]\nprint(long)` | 글자 수가 5개 이상인 단어만 추출합니다. |
| 4 | `logs = ["ERR", "OK", "ERR"]\nerr = [l for l in logs if l == "ERR"]\nprint(err)` | 특정 키워드(ERR)만 포함된 리스트를 만듭니다. |
| 5 | `nums = [1, 2, 3, 4, 10]\nsm = [n for n in nums if n < 5]\nprint(sm)` | 5 미만의 숫자로만 구성된 새 목록을 뽑습니다. |
| 6 | `data = ["Alice", "Bob", "Alex"]\na_name = [n for n in data if n[0] == "A"]\nprint(a_name)` | 이름의 첫 글자가 'A'인 명단만 생성합니다. |
| 7 | `mixed = [1, "A", 2, "B"]\nnums = [x for x in mixed if type(x) == int]\nprint(nums)` | 혼합 리스트에서 숫자(int) 타입만 골라냅니다. |
| 8 | `files = ["a.py", "b.txt", "c.py"]\npy = [f for f in files if f.endswith(".py")]\nprint(py)` | 확장자가 `.py`인 파일 목록 전용 리스트를 만듭니다. |
| 9 | `s = "Py3.12"\ndigits = [c for c in s if c.isdigit()]\nprint(digits)` | 문자열에서 숫자 부분만 따로 모아 리스트로 만듭니다. |
| 10 | `ages = [15, 20, 25]\nadults = [a for a in ages if a >= 19]\nprint(adults)` | 나이 목록 중 성인 데이터만 필터링합니다. |
| 20 | `prices = [1200, 800, 2500]\ndc = [p*0.9 for p in prices if p >= 1000]\nprint(dc)` | [복습] 1000원 이상 품목에만 10% 할인을 적용한 리스트를 생성합니다. |
| * | *(세트 11-19 생략, 조건부 필터링 리스트 연습)* | ... |

---

## 🟦 Lv 58: 딕셔너리 컴프리헨션 (Dict Comprehension)
**설명**: 딕셔너리 또한 컴프리헨션을 사용하여 쉽고 빠르게 생성하는 법을 익힙니다.

| Set | 코드 (code) | 가이드 (guide_ko) |
| :--- | :--- | :--- |
| 1 | `sq = {i: i**2 for i in range(1, 4)}\nprint(sq)` | `{1:1, 2:4, 3:9}` 와 같이 제곱값을 가진 딕셔너리를 만듭니다. |
| 2 | `names = ["a", "b"]\nd = {n: n.upper() for n in names}\nprint(d)` | 이름을 키로, 대문자 이름을 값으로 하는 딕셔너리를 만듭니다. |
| 3 | `items = ["App", "Web"]\nlen_d = {i: len(i) for i in items}\nprint(len_d)` | 단어를 키로, 글자 길이를 값으로 저장합니다. |
| 4 | `nums = [1, 2, 3]\nd = {n: "Num" for n in nums}\nprint(d)` | 모든 키에 동일한 문자열 값을 할당해 딕셔너리를 초기화합니다. |
| 5 | `scores = {"A": 90, "B": 80}\nup = {k: v+5 for k, v in scores.items()}\nprint(up)` | 기존 딕셔너리 값들에 가중치를 더해 새 딕셔너리를 만듭니다. |
| 6 | `data = [10, 20, 30]\nidx_d = {v: i for i, v in enumerate(data)}\nprint(idx_d)` | 데이터 값을 키로, 해당 위치(인덱스)를 값으로 뒤집어 저장합니다. |
| 7 | `scores = {"Math": 90, "Eng": 50}\npassed = {k: v for k, v in scores.items() if v >= 80}\nprint(passed)` | 80점 이상의 과목 정보만 필터링한 새 딕셔너리를 만듭니다. |
| 8 | `colors = ["red", "blue"]\nd = {c: c[0] for c in colors}\nprint(d)` | 색상 이름과 첫 글자를 매핑합니다. |
| 9 | `keys = ["ID", "PW"]\nvals = ["admin", "123"]\nd = {keys[i]: vals[i] for i in range(len(keys))}` | 두 리스트를 인덱스로 참조하여 하나의 딕셔너리로 결합합니다. |
| 10 | `s = "abc"\nd = {c: ord(c) for c in s}\nprint(d)` | 문자를 키로, 아스키 코드값을 값으로 저장합니다. |
| 20 | `user = {"A": 10, "B": -5, "C": 20}\nclean = {k: v for k, v in user.items() if v > 0}\nprint(clean)` | [복습] 기존 유저 데이터 중 양수 수치만 남기고 딕셔너리를 정제합니다. |
| * | *(세트 11-19 생략, 딕셔너리 컴프리헨션 응용 연습)* | ... |

---

## 🟦 Lv 59: 복합 조건 컴프리헨션 (Ternary & Nested)
**설명**: `if-else` 구조를 컴프리헨션 안에 넣어 데이터를 변형하거나 필터링하는 고급 기법을 익힙니다.

| Set | 코드 (code) | 가이드 (guide_ko) |
| :--- | :--- | :--- |
| 1 | `n = [1, 2, 3, 4]\nres = ["Even" if i % 2 == 0 else "Odd" for i in n]\nprint(res)` | `if-else`를 앞에 써서 짝수/홀수 판별 결과를 리스트에 담습니다. |
| 2 | `scores = [90, 50, 80]\nres = ["Pass" if s >= 80 else "Fail" for s in scores]\nprint(res)` | 점수 리스트를 합격/불합격 여부 리스트로 변환합니다. |
| 3 | `v = [1, -5, 10, -2]\nres = [i if i > 0 else 0 for i in v]\nprint(res)` | 음수 데이터는 모두 0으로 치환하여 리스트를 만듭니다. |
| 4 | `txt = ["hi", "python", "go"]\nres = [s if len(s) > 2 else "Short" for s in txt]\nprint(res)` | 짧은 단어는 "Short"로 표시하는 규칙을 적용합니다. |
| 5 | `m = [[1, 2], [3, 4]]\nflat = [val for row in m for val in row]\nprint(flat)` | 이중 반복문을 한 줄로 써서 2차원 리스트를 1차원으로 평평하게 폅니다. |
| 6 | `coords = [(x, y) for x in range(2) for y in range(2)]\nprint(coords)` | (x, y) 형태의 좌표쌍들을 리스트 컴프리헨션으로 생성합니다. |
| 7 | `n = [1, 2, 3, 4, 10]\nres = [i for i in n if i > 2 if i % 2 == 0]\nprint(res)` | `if` 조건을 연속해서 써서 2보다 크면서 짝수인 값만 걸러냅니다. |
| 8 | `data = {"a": 15, "b": 5}\nres = {k: ("High" if v >= 10 else "Low") for k, v in data.items()}\nprint(res)` | 딕셔너리 값들을 대/소 구분에 따라 문구로 변환합니다. |
| 9 | `words = ["A", "B"]\ncombined = [c + str(i) for c in words for i in range(1, 3)]\nprint(combined)` | 수평/수직 조합을 생성하여 명단(A1, A2, B1, B2)을 만듭니다. |
| 10 | `nums = [1, 2, 3]\nres = [[i, i**2] for i in nums]\nprint(res)` | 숫자를 `[원본, 제곱]` 형태의 리스트들로 감싸 중첩 리스트를 만듭니다. |
| 20 | `user = [95, 45, 80]\nfinal = [f"OK({s})" if s >= 80 else "RE(0)" for s in user]\nprint(final)` | [복습] 점수에 따라 승인 메시지나 재시험 메시지를 조건부로 생성합니다. |
| * | *(세트 11-19 생략, 복합 컴프리헨션 구조 연습)* | ... |

---

## 🟦 Lv 60: [Challenge] 데이터 변환 마스터 (Transformer)
**설명**: [챌린지] 지금까지 배운 제어문과 컴프리헨션을 총동원하여 복잡한 원본 데이터를 가공합니다.

| Set | 코드 (code) | 가이드 (guide_ko) |
| :--- | :--- | :--- |
| 1 | `data = [12, -5, 30, -1, 15]\nclean = [d for d in data if d > 0]\nprint(f"AVG: {sum(clean)/len(clean)}")` | 양수만 뽑아내어 그 평균을 구하는 컴프리헨션 활용 예제입니다. |
| 2 | `ids = ["user-1", "user-2", "guest-1"]\nusers = [i for i in ids if "guest" not in i]\nprint(users)` | 특정 그룹(guest)을 제외한 유저 명단을 정제합니다. |
| 3 | `prices = [1000, 2000, 500]\nfinal = [p * 0.9 if p >= 1000 else p for p in prices]\nprint(final)` | 1000원 이상에만 일괄 10% 할인을 적용한 최종 가격 목록입니다. |
| 4 | `names = ["alice", "bob", "charlie"]\nd = {n.capitalize(): len(n) for n in names}\nprint(d)` | 이름을 첫 글자만 대문자로 바꾸고 이름 길이를 저장하는 딕셔너리입니다. |
| 5 | `log = "2026-01-07,100,True,Admin"\nclean = [x.strip() for x in log.split(",")]\nprint(clean)` | 콤마 구분 로그를 분해한 뒤 각 요소의 공백까지 말끔히 제거합니다. |
| 6 | `nums = [1, 2, 3, 4, 5]\nres = [i for i in nums if i % 2 != 0]\nprint(f"Odds Count: {len(res)}")` | 홀수만 골라낸 뒤 그 개수를 집계합니다. |
| 7 | `grid = [[i for i in range(3)] for _ in range(3)]\nprint(grid[0])` | 리스트 컴프리헨션 안에 컴프리헨션을 넣어 3x3 격자를 생성합니다. |
| 8 | `source = {"cpu": 0.5, "ram": 0.8}\ncheck = {k: "ALRT" for k, v in source.items() if v >= 0.7}\nprint(check)` | 기준치를 넘는 항목만 추출하여 경고(ALRT) 메시지로 변환합니다. |
| 9 | `txt = "Hello World From Python"\nres = [w[0] for w in txt.split()]\nprint("".join(res))` | 문장의 각 단어 첫 글자만 따서 약어(Acronym)를 만듭니다. |
| 10 | `data = [1, "None", 2, "None", 3]\nclean = [x for x in data if x != "None"]\nprint(sum(clean))` | 문자열 형태 등으로 섞여 들어온 결측치를 제거하고 합계를 구합니다. |
| 20 | `raw = [10, 20, 30, 40, 50]\nmid_fix = [d for d in raw if 15 < d < 45]\nprint(f"Target: {mid_fix} Sum: {sum(mid_fix)}")` | [종합] 특정 수치 범위에 포함된 값들만 추출한 뒤 합계를 계산합니다. |
| * | *(세트 11-19 생략, 데이터 정제/변환 종합 연습)* | ... |
