# Phase 4: 함수와 프로그램 모듈화 (Lv 61 - 65) 상세 세트

각 레벨은 20개의 연습 세트로 구성됩니다.

---

## 🟦 Lv 61: 함수 정의와 호출 기초 (Functions)
**설명**: 반복되는 코드를 이름 붙여 묶어두고(def), 필요할 때 불러서(call) 사용하는 법을 익힙니다.

| Set | 코드 (code) | 가이드 (guide_ko) |
| :--- | :--- | :--- |
| 1 | `def hello():\n    print("Hello, Python!")\nhello()` | `def` 키워드로 함수를 정의하고, 이름 뒤에 괄호를 붙여 호출합니다. |
| 2 | `def greet():\n    print("Welcome to TypingCode")\n    print("Happy Coding!")\ngreet()` | 함수 안에 여러 줄의 코드를 넣어 한꺼번에 실행할 수 있습니다. |
| 3 | `def draw_line():\n    print("-" * 20)\ndraw_line()\nprint("Content")\ndraw_line()` | 구분선을 그리는 기능을 함수화하여 재사용성을 높인 예제입니다. |
| 4 | `def start_sys():\n    print("System Booting...")\nstart_sys()\nstart_sys()` | 필요할 때마다 함수를 여러 번 호출할 수 있습니다. |
| 5 | `def alert():\n    print("!!! WARNING !!!")\n# alert() ` | 함수는 정의만 해두고 호출하지 않으면 실행되지 않습니다. |
| 6 | `def say_hi():\n    msg = "Hi"\n    print(msg)\nsay_hi()` | 함수 내부에서 변수를 선언하고 사용할 수 있습니다. |
| 7 | `def show_ver():\n    v = 1.2\n    print(f"Version: {v}")\nshow_ver()` | f-string을 활용한 메시지 출력기 함수입니다. |
| 8 | `def run():\n    pass\nrun()` | `pass`는 함수 본문을 나중에 작성하고 싶을 때 쓰는 자리 채우기용입니다. |
| 9 | `def enter():\n    print("\\n" * 2)\nprint("A")\nenter()\nprint("B")` | 빈 줄을 여러 번 출력하는 유틸리티 함수입니다. |
| 10 | `def log_start():\n    print("[LOG] Process Started")\nlog_start()` | 로그 기록 시작을 알리는 알림 함수입니다. |
| 20 | `def end_program():\n    print("=" * 10)\n    print("FINISHED")\n    print("=" * 10)\nend_program()` | [복습] 프로그램 종료 시 일정한 박스 모양을 출력하는 마무리 함수입니다. |
| * | *(세트 11-19 생략, 함수 기본 정의 및 호출 연습)* | ... |

---

## 🟦 Lv 62: 매개변수와 인자 (Parameters)
**설명**: 함수 외부에서 데이터를 전달받아 처리하는 유연한 함수를 만드는 법을 익힙니다.

| Set | 코드 (code) | 가이드 (guide_ko) |
| :--- | :--- | :--- |
| 1 | `def greet(name):\n    print(f"Hi, {name}")\ngreet("Alice")` | `name`은 외부 값을 받아오는 '매개변수'입니다. |
| 2 | `def add(a, b):\n    print(f"Sum: {a + b}")\nadd(10, 20)` | 여러 개의 데이터를 콤마로 구분하여 전달받을 수 있습니다. |
| 3 | `def repeat_msg(msg, cnt):\n    print(msg * cnt)\nrepeat_msg("Go! ", 3)` | 메시지와 반복 횟수를 받아 동적으로 출력합니다. |
| 4 | `def show_info(id, ver):\n    print(f"ID:{id}, Ver:{ver}")\nshow_info("admin", 2.0)` | 서로 다른 타입의 데이터들을 인자로 받아 처리합니다. |
| 5 | `def multi_print(char, size):\n    print(char * size)\nmulti_print("#", 5)` | 장식 문자와 길이를 받아 출력을 제어하는 함수입니다. |
| 6 | `def area(w, h):\n    print(f"Area: {w * h}")\narea(5, 4)` | 가로와 세로 길이를 받아 넓이를 계산하는 함수입니다. |
| 7 | `def check_score(s):\n    if s >= 80:\n        print("Pass")\n    else:\n        print("Fail")\ncheck_score(85)` | 입력받은 값에 따라 조건문을 실행하는 함수입니다. |
| 8 | `def welcome(u):\n    print(f"Welcome, {u.title()}")\nwelcome("antigravity")` | 입력받은 문자열을 가공하여 출력하는 함수입니다. |
| 9 | `def power(n):\n    print(n ** 2)\npower(10)` | 숫자를 받아 제곱값을 출력합니다. |
| 10 | `def introduce(name, age):\n    print(f"{name} is {age} years old.")\nintroduce("Bob", 20)` | 두 가지 정보를 조합하여 문장을 구성하는 함수입니다. |
| 20 | `def calc_tax(p, rate):\n    print(f"Tax: {p * rate:.0f}")\ncalc_tax(5000, 0.1)` | [복습] 가격과 세율을 받아 세액을 계산한 뒤 정수형으로 보여주는 함수입니다. |
| * | *(세트 11-19 생략, 매개변수 전달 및 활용 연습)* | ... |

---

## 🟦 Lv 63: 반환값 (Return Statement)
**설명**: 함수의 처리 결과를 밖으로 돌려보내어 대입하거나 재사용하는 법을 익힙니다.

| Set | 코드 (code) | 가이드 (guide_ko) |
| :--- | :--- | :--- |
| 1 | `def get_sum(a, b):\n    return a + b\nres = get_sum(10, 5)\nprint(res)` | `return`은 결과를 함수 밖으로 던져주어 변수에 저장할 수 있게 합니다. |
| 2 | `def square(n):\n    return n * n\nprint(square(5) + 10)` | 반환된 값을 다른 연산과 즉시 결합할 수도 있습니다. |
| 3 | `def is_adult(age):\n    return age >= 19\nprint(is_adult(20))` | 판단의 결과(불리언)를 반환하는 함수입니다. |
| 4 | `def get_greeting(n):\n    return f"Hello, {n}"\nmsg = get_greeting("Dev")\nprint(msg)` | 가공된 문자열을 반환하여 외부에서 활용합니다. |
| 5 | `def calc(a, b):\n    return a+b, a-b\nsum_v, diff_v = calc(10, 5)\nprint(sum_v, diff_v)` | 콤마로 여러 값을 나열하면 튜플(Tuple) 형태로 여러 값을 반환합니다. |
| 6 | `def find_max(a, b):\n    return a if a > b else b\nprint(find_max(10, 20))` | 조건에 따른 결과값을 선택해서 반환하는 예시입니다. |
| 7 | `def get_first(lst):\n    return lst[0]\nprint(get_first([9, 8, 7]))` | 리스트를 인자로 받아 첫 번째 요소를 반환합니다. |
| 8 | `def check_empty(val):\n    if not val:\n        return "Empty"\n    return "Exist"\nprint(check_empty(""))` | 중간에 `return`이 실행되면 함수는 즉시 그곳에서 종료됩니다. |
| 9 | `def get_status(code):\n    d = {200: "OK", 404: "Not Found"}\n    return d.get(code, "Unknown")\nprint(get_status(404))` | 딕셔너리를 활용해 결과 문구를 매칭하여 반환합니다. |
| 10 | `def repeat(n, c):\n    return n * c\nprint(repeat("!", 5))` | 문자열을 반복 시켜 그 결과를 돌려받습니다. |
| 20 | `def avg3(a, b, c):\n    return (a + b + c) / 3\nprint(f"Result: {avg3(10, 20, 30):.1f}")` | [복습] 세 숫자의 평균을 구하고 소수점 제한을 걸어 출력합니다. |
| * | *(세트 11-19 생략, 다양한 반환값 패턴 연습)* | ... |

---

## 🟦 Lv 64: 기본값 매개변수 (Default Arguments)
**설명**: 인자를 생략했을 때 사용될 기본값을 미리 설정하는 법을 익힙니다.

| Set | 코드 (code) | 가이드 (guide_ko) |
| :--- | :--- | :--- |
| 1 | `def greet(name="User"):\n    print(f"Hello, {name}")\ngreet()\ngreet("Alice")` | `greet()` 처럼 인자 없이 부르면 기본값 "User"가 사용됩니다. |
| 2 | `def power(n, p=2):\n    print(n ** p)\npower(10)\npower(2, 10)` | 거듭제곱 지수의 기본값을 2로 설정합니다. 필요시에만 다른 값을 넣습니다. |
| 3 | `def log(msg, level="INFO"):\n    print(f"[{level}] {msg}")\nlog("Start")` | 로그 레벨의 기본값을 "INFO"로 설정한 예제입니다. |
| 4 | `def box(w=10, h=5, char="*"):\n    print(f"{char}" * w)` | 여러 매개변수에 모두 기본값을 줄 수 있습니다. |
| 5 | `def ask(q, retries=3, hint="Yes/No"):\n    print(q, hint, retries)` | 필수 인자(`q`)는 앞에, 기본값 인자들은 뒤에 배치해야 합니다. |
| 6 | `def draw(x, y, color="black"):\n    print(f"Draw at {x},{y} color {color}")\ndraw(10, 20)` | 좌표는 필수이고 색상은 옵션인 상황을 표현합니다. |
| 7 | `def connect(host, port=8080):\n    print(f"URL: {host}:{port}")\nconnect("google.com")` | 서버 포트 기본값 설정 예시입니다. |
| 8 | `def say(m, end="!"):\n    print(m, end=end)\nsay("Hi")` | 파이썬 내장 `print` 함수의 `end`와 같은 원리입니다. |
| 9 | `def profile(name, age=0):\n    print(f"Name:{name}, Age:{age if age else 'N/A'}")\nprofile("Kim")` | 나이가 입력되지 않았을 때 표시를 조정합니다. |
| 10 | `def msg(t, cnt=1):\n    for _ in range(cnt): print(t)\nmsg("Go!")` | 반복 횟수를 생략하면 1번만 실행하도록 기본값을 줍니다. |
| 20 | `def bar(len=10, filler="-"):\n    print(filler * len)\nbar(5, "=")` | [복습] 원하는 가로 길이와 채울 문자를 받아 구분선을 그리는 함수입니다. |
| * | *(세트 11-19 생략, 기본값 매개변수 변형 연습)* | ... |

---

## 🟦 Lv 65: [Challenge] 도형 계산기 모듈 (Geometry)
**설명**: [챌린지] 다양한 도형의 넓이와 둘레를 계산하는 함수 세트를 구성합니다.

| Set | 코드 (code) | 가이드 (guide_ko) |
| :--- | :--- | :--- |
| 1 | `def get_square_area(s):\n    return s * s\nprint(get_square_area(4))` | 정사각형 넓이를 구하는 함수를 호출합니다. |
| 2 | `def get_rect_area(w, h):\n    return w * h\nprint(get_rect_area(5, 10))` | 직사각형 넓이 계산은 두 인자를 받습니다. |
| 3 | `def get_circle_area(r, pi=3.14):\n    return pi * (r ** 2)\nprint(get_circle_area(10))` | 원주율 기본값을 설정하고 원의 넓이를 구합니다. |
| 4 | `def get_rect_info(w, h):\n    area = w * h\n    peri = (w + h) * 2\n    return area, peri\na, p = get_rect_info(4, 5)\nprint(f"A:{a}, P:{p}")` | 넓이와 둘레를 동시에 구하여 반환합니다. |
| 5 | `def is_valid_input(val):\n    return val > 0\nif is_valid_input(10):\n    print("Can Calculate")` | 입력값이 유효한(양수)지 확인하는 보조 함수입니다. |
| 6 | `def draw_rect(w, h, char="#"):\n    for _ in range(h):\n        print(char * w)\ndraw_rect(5, 2)` | 특정 문자로 직사각형의 형태를 출력하는 시각화 함수입니다. |
| 7 | `def get_triangle_area(b, h):\n    return b * h / 2\nprint(f"Triangle: {get_triangle_area(10, 5)}")` | 삼각형 넓이 공식 함수입니다. |
| 8 | `def convert_unit(val, to_mm=True):\n    return val * 10 if to_mm else val / 10\nprint(convert_unit(5))` | cm를 mm로 바꾸거나 그 반대를 수행하는 변환기입니다. |
| 9 | `def get_box_volume(w, h, d):\n    return w * h * d\nprint(get_box_volume(2, 3, 4))` | 세 가지 차원 데이터를 받아 부피를 계산합니다. |
| 10 | `def log_calc(name, res):\n    print(f"[CALC] {name} Result: {res}")\nlog_calc("Circle", 314.0)` | 계산 결과를 로그 형식으로 남겨주는 알림 함수입니다. |
| 20 | `def run_geometry_suite(s1, s2):\n    a = get_rect_area(s1, s2)\n    p = (s1 + s2) * 2\n    log_calc("Full Suite", f"A:{a} P:{p}")\nrun_geometry_suite(10, 5)` | [종합] 기존에 만든 함수들을 내부에서 호출하여 전체 보고서를 작성하는 고급 함수입니다. |
| * | *(세트 11-19 생략, 기하학 계산 시뮬레이션)* | ... |
