# Phase 4: 함수와 프로그램 모듈화 (Lv 71 - 75) 상세 세트

각 레벨은 20개의 연습 세트로 구성됩니다.

---

## 🟦 Lv 71: 람다 함수 (Lambda)
**설명**: 이름 없이 간결하게 한 줄로 정의하여 사용하는 '익명 함수' 문법을 익힙니다.

| Set | 코드 (code) | 가이드 (guide_ko) |
| :--- | :--- | :--- |
| 1 | `add = lambda a, b: a + b\nprint(add(10, 20))` | `lambda`는 `def` 없이 한 줄로 함수를 정의합니다. |
| 2 | `sq = lambda x: x ** 2\nprint(sq(5))` | 인자를 하나 받아 제곱을 계산하는 간단한 람다식입니다. |
| 3 | `is_even = lambda n: n % 2 == 0\nprint(is_even(10))` | 짝수 여부를 판단해 결과를 참/거짓으로 돌려주는 람다식입니다. |
| 4 | `upper = lambda s: s.upper()\nprint(upper("python"))` | 문자열을 대문자로 변환하는 기능을 람다로 구현합니다. |
| 5 | `greet = lambda n: f"Hi, {n}"\nprint(greet("Alice"))` | f-string을 사용한 인사 문구를 생성하는 람다식입니다. |
| 6 | `multi = lambda a, b, c: a * b * c\nprint(multi(1, 2, 3))` | 세 개의 인자를 받아 곱한 결과를 리턴합니다. |
| 7 | `check = lambda x: "High" if x > 50 else "Low"\nprint(check(70))` | 람다식 내부에서 `if-else` 삼항 연산자를 사용할 수 있습니다. |
| 8 | `full_name = lambda f, l: f"{f} {l}"\nprint(full_name("Guido", "Rossum"))` | 이름과 성을 합쳐주는 람다 예시입니다. |
| 9 | `data = [("A", 10), ("B", 5)]\ndata.sort(key=lambda x: x[1])\nprint(data)` | `sort` 등의 함수에서 정렬 기준(key)을 정할 때 람다가 자주 쓰입니다. |
| 10 | `f = lambda: print("No Args")\nf()` | 인자가 없는 람다 함수도 정의하여 사용할 수 있습니다. |
| 20 | `p = lambda x, rate=0.1: x * rate\nprint(f"Tax: {p(1000):.0f}")` | [복습] 기본값 매개변수를 가진 람다 함수로 세금을 계산합니다. |
| * | *(세트 11-19 생략, 람다 익명 함수 패턴 연습)* | ... |

---

## 🟦 Lv 72: 맵 함수 (map)
**설명**: 리스트나 튜플의 모든 요소에 특정 함수를 일괄 적용하는 효율적인 기법을 익힙니다.

| Set | 코드 (code) | 가이드 (guide_ko) |
| :--- | :--- | :--- |
| 1 | `n = [1, 2, 3]\nres = map(lambda x: x * 2, n)\nprint(list(res))` | 리스트의 모든 숫자를 2배씩 늘려줍니다. |
| 2 | `s = ["a", "b", "c"]\nres = map(str.upper, s)\nprint(list(res))` | 모든 문자열 요소를 한꺼번에 대문자로 변환합니다. |
| 3 | `prices = ["100", "200"]\nres = map(int, prices)\nprint(sum(res))` | 문자열 숫자 리스트를 정수형으로 일괄 변환하여 합계를 구합니다. |
| 4 | `n = [1.2, 5.7, 3.1]\nres = map(round, n)\nprint(list(res))` | 모든 실수를 반올림하여 정수 리스트로 만듭니다. |
| 5 | `words = ["Py", "Code"]\nres = map(len, words)\nprint(list(res))` | 리스트 내 각 단어의 글자 수를 리스트로 뽑아냅니다. |
| 6 | `def deco(s): return f"#{s}"\ntags = ["dev", "py"]\nprint(list(map(deco, tags)))` | 직접 정의한 사용자 함수를 `map`에 적용할 수 있습니다. |
| 7 | `r1 = [1, 2, 3]\nr2 = [10, 20, 30]\nres = map(lambda x, y: x + y, r1, r2)\nprint(list(res))` | 두 개의 리스트를 동시에 받아 각각의 요소를 더해줍니다. |
| 8 | `v = [-10, 5, -2]\nres = map(abs, v)\nprint(list(res))` | 모든 리스트 요소를 절대값으로 변환합니다. |
| 9 | `s = "12345"\nres = map(int, s)\nprint(list(res))` | 문자열을 `map`에 넣으면 글자 하나하나를 숫자로 쪼개줍니다. |
| 10 | `data = [" apple ", " banana "]\nres = map(str.strip, data)\nprint(list(res))` | 여러 문자열의 양쪽 공백을 한꺼번에 제거합니다. |
| 20 | `prices = [1000, 2000, 3000]\nfinal = map(lambda p: int(p * 0.9), prices)\nprint(f"DC Prices: {list(final)}")` | [복습] 가격 리스트의 모든 품목에 10% 할인을 적용한 뒤 정수로 변환합니다. |
| * | *(세트 11-19 생략, map 연산 일괄 적용 연습)* | ... |

---

## 🟦 Lv 73: 필터 함수 (filter)
**설명**: 리스트에서 특정 조건에 부합하는 데이터만 쏙쏙 골라내는 법을 익힙니다.

| Set | 코드 (code) | 가이드 (guide_ko) |
| :--- | :--- | :--- |
| 1 | `n = [1, 2, 3, 4, 5, 6]\nres = filter(lambda x: x % 2 == 0, n)\nprint(list(res))` | 리스트에서 짝수만 걸러내어 새로운 목록을 만듭니다. |
| 2 | `data = [10, 55, 30, 80, 20]\nres = filter(lambda x: x > 50, data)\nprint(list(res))` | 50보다 큰 숫자들만 필터링합니다. |
| 3 | `names = ["Alice", "Bob", "Alex"]\nres = filter(lambda n: n.startswith("A"), names)\nprint(list(res))` | 이름이 'A'로 시작하는 사람만 추출합니다. |
| 4 | `mix = [1, "A", 2, "B"]\nres = filter(lambda x: type(x) == int, mix)\nprint(list(res))` | 리스트에서 특정 타입(정수)인 데이터만 골라냅니다. |
| 5 | `words = ["cat", "dog", "elephant"]\nres = filter(lambda w: len(w) > 3, words)\nprint(list(res))` | 글자 수가 3개를 초과하는 단어만 필터링합니다. |
| 6 | `vals = [0, 1, False, True, ""]\nres = filter(None, vals)\nprint(list(res))` | `filter`에 `None`을 주면 값이 참(Truthy)인 것들만 남깁니다. |
| 7 | `logs = ["ERR01", "OK", "ERR02"]\nres = filter(lambda l: "ERR" in l, logs)\nprint(list(res))` | 에러 키워드가 포함된 로그만 걸러냅니다. |
| 8 | `scores = [85, 40, 92, 55]\nres = filter(lambda s: s >= 60, scores)\nprint(f"Pass count: {len(list(res))}")` | 과락 점수를 제외한 합격한 점수들의 개수를 구합니다. |
| 9 | `files = ["a.py", "b.txt", "c.py"]\nres = filter(lambda f: f.endswith(".py"), files)\nprint(list(res))` | 파이썬 확장자 파일만 골라내는 로직입니다. |
| 10 | `ages = [15, 20, 25]\nres = filter(lambda a: a >= 19, ages)\nprint(f"Min Adult: {min(res)}")` | 성인들만 골라낸 뒤 그중 가장 어린 나이를 찾습니다. |
| 20 | `user = ["admin", "root", "dev_01"]\nvalid = filter(lambda u: len(u) >= 5, user)\nprint(f"Valid IDs: {list(valid)}")` | [복습] 아이디 길이가 5자 이상인 유효한 사용자 목록만 필터링합니다. |
| * | *(세트 11-19 생략, 데이터 조건부 필터링 연습)* | ... |

---

## 🟦 Lv 74: 짚 함수 (zip)
**설명**: 여러 개의 리스트를 병렬로 묶어 동시에 하나씩 꺼내어 사용하는 법을 익힙니다.

| Set | 코드 (code) | 가이드 (guide_ko) |
| :--- | :--- | :--- |
| 1 | `keys = ["ID", "PW"]\nvals = ["admin", "123"]\nres = zip(keys, vals)\nprint(list(res))` | 두 리스트의 같은 위치 아이템을 튜플로 묶어줍니다. |
| 2 | `names = ["Alice", "Bob"]\nage = [25, 30]\nfor n, a in zip(names, age):\n    print(f"{n} is {a}")` | `zip`을 `for` 문과 함께 쓰면 두 데이터를 동시에 순회하기 편리합니다. |
| 3 | `ids = [1, 2]\nnms = ["A", "B"]\nd = dict(zip(ids, nms))\nprint(d)` | `zip` 결과를 `dict()`로 감싸면 즉시 딕셔너리로 변환됩니다. |
| 4 | `a = [1, 2, 3]\nb = [10, 20]\nprint(list(zip(a, b)))` | 대상 리스트들의 길이가 다를 경우 가장 짧은 것 기준으로 멈춥니다. |
| 5 | `items = ["Phone", "Pad"]\nprice = [1000, 2000]\nqty = [5, 2]\nfor i, p, q in zip(items, price, qty):\n    print(f"{i}: {p * q}")` | 3개 이상의 리스트도 한꺼번에 묶어서 계산할 수 있습니다. |
| 6 | `chars = "ABC"\nnums = [1, 2, 3]\nprint(list(zip(chars, nums)))` | 문자열과 리스트도 묶음 처리가 가능합니다. |
| 7 | `pairs = [(1, "A"), (2, "B")]\na, b = zip(*pairs)\nprint(a, b)` | `zip(*)` 형식을 사용하면 묶여있던 튜플 리스트를 다시 분리(Unzip)합니다. |
| 8 | `eng = ["one", "two"]\nkor = ["하나", "둘"]\nprint(list(zip(eng, kor)))` | 영단어와 한글 의미를 짝지어 단어장 데이터를 만듭니다. |
| 9 | `s1 = [10, 20]\ns2 = [5, 15]\nres = [a - b for a, b in zip(s1, s2)]\nprint(res)` | 두 리스트 요소 간의 차이를 구할 때 컴프리헨션과 결합합니다. |
| 10 | `head = ["Name", "Job"]\nbody = ["Alice", "Dev"]\nprint(dict(zip(head, body)))` | 표의 헤더와 데이터 열을 묶어 정보를 구조화합니다. |
| 20 | `h, m, s = [9, 10], [5, 12], [2, 7]\nfor t in zip(h, m, s):\n    print(f"Time - {t[0]:02d}:{t[1]:02d}:{t[2]:02d}")` | [복습] 시, 분, 초 리스트를 묶어 정제된 시간 포맷으로 여러 줄 출력합니다. |
| * | *(세트 11-19 생략, 병렬 순회 및 데이터 매핑 연습)* | ... |

---

## 🟦 Lv 75: [Challenge] 데이터 파이프라인 (Stream)
**설명**: [챌린지] map, filter, lambda, zip을 결합하여 복잡한 데이터를 가공하는 파이프라인을 만듭니다.

| Set | 코드 (code) | 가이드 (guide_ko) |
| :--- | :--- | :--- |
| 1 | `data = [1, 2, 3, 4, 5]\nres = map(lambda x: x**2, filter(lambda x: x % 2 == 0, data))\nprint(list(res))` | 짝수만 골라내어(filter) 각각을 제곱(map)하는 연속 작업입니다. |
| 2 | `raw = [" 10 ", " 20 ", " -5 "]\nres = map(int, map(str.strip, raw))\nprint(list(res))` | 공백을 지우고 다시 숫자로 바꾸는 2단계 변환 파이프라인입니다. |
| 3 | `ids = ["a", "b"]\nscores = [90, 45]\nvalid = filter(lambda x: x[1] >= 80, zip(ids, scores))\nprint(list(valid))` | 정보를 묶은(zip) 뒤 특정 조건(점수)으로 걸러냅니다(filter). |
| 4 | `txt = ["Hi", "Hello", "Bye"]\nres = filter(lambda s: "H" in s, map(str.upper, txt))\nprint(list(res))` | 모두 대문자로 바꾼 뒤 'H'가 포함된 단어만 남깁니다. |
| 5 | `prices = [1200, 800, 5000]\nfinal = filter(lambda x: x >= 1000, map(lambda p: p * 1.1, prices))\nprint(list(final))` | 세금을 포함한 가격을 구한 뒤 그중 비싼 물건만 추출합니다. |
| 6 | `names = ["Alice", "Bob"]\nres = "".join(map(lambda n: n[0], names))\nprint(res)` | 이름들의 첫 글자만 모아서 한 단어로 합칩니다. |
| 7 | `data = range(1, 11)\nres = sum(filter(lambda x: x % 3 == 0, data))\nprint(res)` | 1~10 중 3의 배수들의 총합을 함수 조합으로 구합니다. |
| 8 | `u = ["a", "b", "c"]\nchecked = map(lambda x: x + "@ok", u)\nprint(list(checked))` | 아이디 뒤에 상태 표시를 일괄적으로 덧붙입니다. |
| 9 | `v = [1, 2, 3]\nres = zip(v, map(lambda x: x**2, v))\nprint(dict(res))` | 원본 수와 그 숫자의 제곱수로 된 딕셔너리를 효율적으로 만듭니다. |
| 10 | `nums = [-2, 0, 5, -8]\nclean = filter(lambda x: x != 0, map(abs, nums))\nprint(list(clean))` | 모든 수를 양수로 바꾸고 0은 제외하는 정제 로직입니다. |
| 20 | `user = ["kim", "lee"]\nscore = [95, 80]\npassed = filter(lambda x: x[1] >= 90, zip(user, score))\nfmt = map(lambda x: f"USER:{x[0].upper()}", passed)\nprint(list(fmt))` | [종합] 유저와 점수를 묶고, 90점 이상만 걸러낸 뒤, 이름을 대문자로 포맷팅합니다. |
| * | *(세트 11-19 생략, 고차 함수 조합 및 데이터 스트림 가공)* | ... |
